---
phase: 01-skeleton
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - tests/fixtures/v1-bundle.rshape.txt
  - tests/fixtures/v1-bundle.fixture.ts
  - tests/fixtures/v1-localstorage.fixture.ts
  - tests/fixtures/README.md
  - tests/setup.ts
autonomous: false
requirements:
  - CORE-04
  - CORE-08
  - FOUND-06
tags:
  - fixtures
  - testing
  - vitest
  - crypto
  - storage
  - legacy

must_haves:
  truths:
    - "A v1.0-produced PEM-armored AES-GCM 256 bundle exists at tests/fixtures/v1-bundle.rshape.txt, captured manually via the running /legacy/ app per D-24 + CONTEXT.md specifics"
    - "A typed fixture module at tests/fixtures/v1-bundle.fixture.ts exports ARMORED, PASSPHRASE, EXPECTED_PAYLOAD for the plan 04 crypto round-trip test"
    - "A typed fixture module at tests/fixtures/v1-localstorage.fixture.ts exports V1_LOCALSTORAGE_BLOB for the plan 06 storage hydrate test"
    - "tests/fixtures/README.md documents the regeneration procedure"
    - "tests/setup.ts exists as a placeholder Vitest setup file"
  artifacts:
    - path: "tests/fixtures/v1-bundle.rshape.txt"
      provides: "Raw PEM-armored ciphertext captured from the running /legacy/ app via Share with the known passphrase"
      contains: "-----BEGIN RELATIONSHAPE BUNDLE-----"
    - path: "tests/fixtures/v1-bundle.fixture.ts"
      provides: "Typed exports ARMORED, PASSPHRASE, EXPECTED_PAYLOAD consumed by plan 04 crypto.test.ts"
      exports:
        - "ARMORED"
        - "PASSPHRASE"
        - "EXPECTED_PAYLOAD"
    - path: "tests/fixtures/v1-localstorage.fixture.ts"
      provides: "Typed export V1_LOCALSTORAGE_BLOB consumed by plan 06 storage.test.ts"
      exports:
        - "V1_LOCALSTORAGE_BLOB"
        - "V1_LOCALSTORAGE_PARSED"
    - path: "tests/fixtures/README.md"
      provides: "Step-by-step procedure to regenerate the bundle fixture"
      contains: "Test Subject"
    - path: "tests/setup.ts"
      provides: "Vitest setup placeholder for Phase 2 expansion"
  key_links:
    - from: "tests/fixtures/v1-bundle.fixture.ts"
      to: "tests/fixtures/v1-bundle.rshape.txt"
      via: "Inline string literal OR fs.readFileSync at module load"
      pattern: "ARMORED ="
    - from: "tests/fixtures/v1-bundle.rshape.txt"
      to: "AES-GCM 256 + PBKDF2 SHA-256 × 250 000 envelope"
      via: "PEM-style armor with v1 version line"
      pattern: "-----BEGIN RELATIONSHAPE BUNDLE-----"
    - from: "tests/fixtures/v1-localstorage.fixture.ts"
      to: "v1.0 localStorage blob shape"
      via: "JSON.stringify of an object matching public/legacy/js/storage.js defaults() + saveProfile() + saveResult()"
      pattern: "JSON.stringify"
---

<objective>
Capture the v1.0 encrypted-bundle fixture and the v1.0 localStorage blob fixture that downstream port tests (plan 04 crypto, plan 06 storage) consume. Per D-24, the bundle fixture is a one-time MANUAL capture using the running legacy app at `/legacy/` — Claude cannot generate a v1.0 bundle programmatically because AES-GCM bundles are non-deterministic (random salt + IV per encryption), and EXPECTED_PAYLOAD must match what the v1.0 Share flow actually assembled.

This plan has one human-action checkpoint (user runs `pnpm run dev`, opens `/legacy/index.html`, performs the Share flow with a known passphrase, copies the armored output into `v1-bundle.rshape.txt`, copies the assembled payload into `v1-bundle.fixture.ts`). The surrounding automation (creating the directory, writing the localStorage fixture, writing the README, writing the empty setup.ts) is autonomous.

Purpose: Without `v1-bundle.rshape.txt`, plan 04's crypto round-trip test cannot prove byte-for-byte parity with v1.0 (CORE-04, PWA-04). Without `v1-localstorage.fixture.ts`, plan 06's storage hydrate test cannot prove CORE-08 (loading the new app against a v1.0 blob yields identical in-memory state).

Output: Four files under `tests/fixtures/` + `tests/setup.ts`. All checked into git so future test runs do not need to re-capture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-skeleton/01-CONTEXT.md
@.planning/phases/01-skeleton/01-RESEARCH.md
@.planning/phases/01-skeleton/01-PATTERNS.md
@.planning/phases/01-skeleton/01-VALIDATION.md
@public/legacy/js/crypto.js
@public/legacy/js/storage.js
@public/legacy/js/data.js

The v1.0 bundle envelope shape is LOCKED (from `public/legacy/js/crypto.js:55-70`):
- Outer PEM armor with `-----BEGIN RELATIONSHAPE BUNDLE-----` header, `v1` version line, base64-of-JSON envelope, `-----END RELATIONSHAPE BUNDLE-----` footer
- Inner envelope: `{ kdf: { n: 'PBKDF2', h: 'SHA-256', i: 250000, s: b64_16_byte_salt }, cipher: { n: 'AES-GCM', iv: b64_12_byte_iv }, data: b64_ciphertext }`

The v1.0 localStorage blob shape is LOCKED (from `public/legacy/js/storage.js:7,33-43`):
- Key: `relationshape.v1`
- Shape: `{ profiles: Profile[], results: Result[], imports: Import[], settings: { theme, lang?, fabiMode?, wizardSeen? }, scale: ScaleStep[] }`
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: Scaffold tests/ directory, write the autonomous fixtures (localStorage blob + setup.ts + README skeleton)</name>
  <files>tests/setup.ts, tests/fixtures/v1-localstorage.fixture.ts, tests/fixtures/README.md</files>
  <read_first>
    - public/legacy/js/storage.js (lines 1-50 — confirm KEY, defaults(), cloneScale, uid)
    - public/legacy/js/data.js (lines 1-20 — confirm DEFAULT_SCALE values)
    - .planning/phases/01-skeleton/01-CONTEXT.md (specifics block: "Test Subject" profile, emoji 🌱, color #7c83ff, 3+ enabledCategories, 2 custom items, 1 hidden item)
    - .planning/phases/01-skeleton/01-VALIDATION.md (Wave 0 Requirements list)
  </read_first>
  <action>
    Create `tests/fixtures/` directory if not present.

    Write `tests/setup.ts` with exactly this content (placeholder; Phase 2 may register jest-dom matchers here):

    ```
    // Vitest setup file. Phase 1 has no global setup requirements.
    // Phase 2 may import '@testing-library/jest-dom/vitest' to register DOM matchers.
    export {}
    ```

    Write `tests/fixtures/v1-localstorage.fixture.ts` — synthetic v1.0 blob constructed from the documented shape in `public/legacy/js/storage.js`. Read the legacy `DEFAULT_SCALE` array from `public/legacy/js/data.js:8-16` and embed those EXACT 7 scale steps (key/label/short/value/color/description fields). If the values in `public/legacy/js/data.js` differ from any sample below, the legacy file is authoritative — copy from there.

    File content:

    ```
    // tests/fixtures/v1-localstorage.fixture.ts
    // Synthetic v1.0 localStorage["relationshape.v1"] blob, deterministically constructed.
    // Shape source: public/legacy/js/storage.js KEY='relationshape.v1', defaults(), createProfile(), saveResult().
    // Fixture seed values per .planning/phases/01-skeleton/01-CONTEXT.md <specifics>:
    //   profile: name="Test Subject", emoji="🌱", color="#7c83ff", pronouns="they/them"
    //   one result with 3 enabledCategories, 2 custom items, 1 __hidden item
    //   scale: v1.0 DEFAULT_SCALE verbatim from public/legacy/js/data.js
    // Consumed by src/lib/storage/__tests__/storage.test.ts (plan 06) to verify CORE-08.

    const V1_BLOB_OBJECT = {
      profiles: [
        {
          id: 'profile-test-subject',
          name: 'Test Subject',
          pronouns: 'they/them',
          color: '#7c83ff',
          emoji: '🌱',
          notes: '',
          createdAt: 1715000000000,
        },
      ],
      results: [
        {
          id: 'result-test-fixture',
          profileId: 'profile-test-subject',
          subject: 'Test Subject',
          subjectEmoji: '🌱',
          subjectColor: '#7c83ff',
          answers: {
            connection: {
              'Daily check-ins': { scale: 'maybe', gr: 'Both', note: '' },
              'Shared humour': { scale: 'yes', gr: 'G' },
              __custom: {
                'Reading bedtime stories': { scale: 'maybe', gr: 'Both', note: 'custom item 1' },
              },
              __hidden: {},
            },
            intimacy: {
              'Holding hands': { scale: 'yes', gr: 'Both' },
              __custom: {
                'Surprise hugs': { scale: 'need', gr: 'R' },
              },
              __hidden: {
                'Cuddling on the couch': true,
              },
            },
            partnership: {
              'Long-term plans': { scale: 'not-really', gr: 'G' },
              __custom: {},
              __hidden: {},
            },
          },
          enabledCategories: ['connection', 'intimacy', 'partnership'],
          askedItems: {},
          version: 1,
          createdAt: 1715000100000,
          updatedAt: 1715000200000,
        },
      ],
      imports: [],
      settings: {
        theme: 'auto',
        lang: 'en',
        wizardSeen: true,
      },
      scale: [
        // NOTE: executor must copy these from public/legacy/js/data.js DEFAULT_SCALE exactly.
        // Below is a placeholder structure; replace each entry's color/description if data.js disagrees.
        { key: 'no',         label: 'No',             short: 'No',         value: 0, color: '#264653', description: 'I do not want / agree to this.' },
        { key: 'not-really', label: 'Not really',     short: 'Not really', value: 1, color: '#577590', description: 'I lean against this.' },
        { key: 'maybe',      label: 'Maybe / future', short: 'Maybe',      value: 2, color: '#43aa8b', description: 'Hopefully or maybe in the future.' },
        { key: 'sometimes',  label: 'Sometimes',      short: 'Sometimes',  value: 3, color: '#90be6d', description: 'I am open to this sometimes.' },
        { key: 'yes',        label: 'Yes',            short: 'Yes',        value: 4, color: '#f9c74f', description: 'I am open to this.' },
        { key: 'want',       label: 'Want',           short: 'Want',       value: 5, color: '#f8961e', description: 'I want this.' },
        { key: 'need',       label: 'Need',           short: 'Need',       value: 6, color: '#f3722c', description: 'I need this in my life.' },
      ],
    } as const

    export const V1_LOCALSTORAGE_BLOB: string = JSON.stringify(V1_BLOB_OBJECT)
    export const V1_LOCALSTORAGE_PARSED = V1_BLOB_OBJECT
    ```

    Write `tests/fixtures/README.md` documenting the regeneration procedure per D-24:

    ```
    # Test Fixtures

    These fixtures pin the v1.0 → v2.0 compatibility contract. They are committed to git so future test runs can re-execute without re-capturing.

    ## Files

    - v1-bundle.rshape.txt — PEM-armored AES-GCM 256 encrypted bundle produced by v1.0's Share flow with a known passphrase. Captured manually (see below). Consumed by src/lib/crypto/__tests__/crypto.test.ts (plan 04).
    - v1-bundle.fixture.ts — Typed wrapper exporting { ARMORED, PASSPHRASE, EXPECTED_PAYLOAD }. ARMORED embeds the contents of v1-bundle.rshape.txt inline OR reads it via node:fs.readFileSync at module load.
    - v1-localstorage.fixture.ts — Synthetic v1.0 localStorage["relationshape.v1"] blob constructed from the documented shape in public/legacy/js/storage.js. Consumed by src/lib/storage/__tests__/storage.test.ts (plan 06).

    ## Regeneration procedure (v1-bundle.rshape.txt)

    The encrypted bundle MUST be captured from the running v1.0 app — AES-GCM uses random salts + IVs so the armored output is non-deterministic.

    1. Run pnpm run dev from the repo root. Vite serves the legacy app at http://localhost:5173/legacy/index.html.
    2. Open that URL in a private/incognito browser window (so existing localStorage data does not interfere).
    3. Create a profile with: name = "Test Subject", emoji = "🌱", accent color = "#7c83ff", pronouns = "they/them".
    4. From the profile, create one result. In the category overview, enable at least 3 categories (e.g. connection, intimacy, partnership).
    5. Answer at least one item in each enabled category. Add 2 custom items (one in any category) and mark 1 item as hidden via the v1.0 hide-item affordance.
    6. From the result view, open the Share flow.
    7. Enter passphrase: test-passphrase-fixture-only
    8. Confirm. Copy the entire armored output (including BEGIN/END lines and the v1 version line).
    9. Save the copied text to tests/fixtures/v1-bundle.rshape.txt. Do NOT edit — preserve all whitespace exactly.
    10. Update EXPECTED_PAYLOAD in v1-bundle.fixture.ts to match the exact payload structure the share flow assembled. Easiest: add console.log(JSON.stringify(payload, null, 2)) in public/legacy/js/app.js near the Share submit handler, capture the output, paste it into EXPECTED_PAYLOAD, then remove the console.log.

    ## Why we capture manually

    AES-GCM bundles are non-deterministic — every encryption uses a new random 16-byte salt and 12-byte IV (public/legacy/js/crypto.js:56-57). A v2.0-generated bundle would NOT round-trip to a v1.0-generated one byte-for-byte. The test asserts that v2.0 can DECRYPT a v1.0 bundle (CORE-04, PWA-04) — which requires a real v1.0 bundle pinned in the fixture.

    ## Why localStorage is synthetic

    Unlike the bundle, the localStorage blob shape is fully deterministic — JSON.stringify(defaults()) per public/legacy/js/storage.js:33. Claude can construct a representative blob from the documented shape; no human action required. The plan 06 test asserts the new app's Zustand state matches the parsed blob.

    ## Fixture passphrase disclosure

    The passphrase test-passphrase-fixture-only is INTENTIONALLY committed to git in this README and in v1-bundle.fixture.ts. The fixture bundle contains synthetic data (a profile called "Test Subject" with no PII); the passphrase has no security value outside the fixture. Do NOT reuse this passphrase for real bundles.
    ```

    Critical constraints:
    - Do NOT pre-populate `tests/fixtures/v1-bundle.rshape.txt` or `tests/fixtures/v1-bundle.fixture.ts` in this task — those come from the human-action checkpoint in task 3.2.
    - When copying the scale values into `v1-localstorage.fixture.ts`, read the actual values from `public/legacy/js/data.js` DEFAULT_SCALE and use those — the values shown in the action template above are best-guesses; the file is the source of truth.
    - `tests/setup.ts` must include `export {}` so TypeScript treats it as a module (D-15 strict mode + verbatimModuleSyntax requirement).
    - The scale field shape `{ key, label, short, value, color, description }` is LOCKED (data.js: 8-16). Do not rename fields.

    After writing, run `pnpm run typecheck` to verify the synthetic fixture compiles under D-15 strict mode.
  </action>
  <verify>
    <automated>test -d tests/fixtures && test -f tests/setup.ts && test -f tests/fixtures/v1-localstorage.fixture.ts && test -f tests/fixtures/README.md && grep -c V1_LOCALSTORAGE_BLOB tests/fixtures/v1-localstorage.fixture.ts && grep -c "Test Subject" tests/fixtures/v1-localstorage.fixture.ts && grep -c "#7c83ff" tests/fixtures/v1-localstorage.fixture.ts && grep -c "test-passphrase-fixture-only" tests/fixtures/README.md && grep -c "DEFAULT_SCALE\|scale:" tests/fixtures/v1-localstorage.fixture.ts && pnpm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `test -d tests/fixtures` returns 0
    - `test -f tests/setup.ts` returns 0
    - `test -f tests/fixtures/v1-localstorage.fixture.ts` returns 0
    - `test -f tests/fixtures/README.md` returns 0
    - `grep -c "export const V1_LOCALSTORAGE_BLOB" tests/fixtures/v1-localstorage.fixture.ts` returns 1
    - `grep -c "export const V1_LOCALSTORAGE_PARSED" tests/fixtures/v1-localstorage.fixture.ts` returns 1
    - `grep -c "Test Subject" tests/fixtures/v1-localstorage.fixture.ts` returns at least 1
    - `grep -c "🌱" tests/fixtures/v1-localstorage.fixture.ts` returns at least 1
    - `grep -c "#7c83ff" tests/fixtures/v1-localstorage.fixture.ts` returns at least 1
    - `grep -c "__hidden" tests/fixtures/v1-localstorage.fixture.ts` returns at least 1 (covers the hidden-item semantics requirement)
    - `grep -c "__custom" tests/fixtures/v1-localstorage.fixture.ts` returns at least 1
    - `grep -c "enabledCategories" tests/fixtures/v1-localstorage.fixture.ts` returns 1
    - `grep -c "'no'\|key: 'no'" tests/fixtures/v1-localstorage.fixture.ts` returns at least 1 (scale step from DEFAULT_SCALE)
    - `grep -c "'need'\|key: 'need'" tests/fixtures/v1-localstorage.fixture.ts` returns at least 1 (last scale step)
    - `grep -c "test-passphrase-fixture-only" tests/fixtures/README.md` returns at least 1
    - `grep -c "v1-bundle.rshape.txt" tests/fixtures/README.md` returns at least 1
    - `grep -c "export {}" tests/setup.ts` returns 1 (module marker for verbatimModuleSyntax)
    - `pnpm run typecheck` exits 0 (D-15 strict mode passes on the synthetic fixture)
  </acceptance_criteria>
  <done>
    The tests/fixtures/ directory exists; tests/setup.ts is a valid TypeScript module; tests/fixtures/v1-localstorage.fixture.ts contains a typed V1_LOCALSTORAGE_BLOB matching the v1.0 shape (with Test Subject profile, 3 enabled categories, custom items, hidden items, and the v1.0 DEFAULT_SCALE 7 steps); tests/fixtures/README.md documents the manual bundle capture procedure with passphrase test-passphrase-fixture-only. Plan 06 can now import V1_LOCALSTORAGE_BLOB; plan 04 still needs task 3.2 to complete before its crypto test can run.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3.2: HUMAN ACTION — Capture v1.0 bundle fixture from the running legacy app</name>
  <what-built>
    Task 3.1 created the fixtures directory and the synthetic localStorage fixture. The encrypted-bundle fixture (`tests/fixtures/v1-bundle.rshape.txt`) and its TS wrapper (`tests/fixtures/v1-bundle.fixture.ts`) MUST be captured by hand because AES-GCM is non-deterministic — Claude cannot generate a v1.0 bundle that round-trips against a v1.0 decrypt. The exact procedure is documented in `tests/fixtures/README.md` written in task 3.1.
  </what-built>
  <how-to-verify>
    Open a terminal in the repo root and run:

    ```
    pnpm run dev
    ```

    Vite starts on http://localhost:5173/. Then:

    1. Open `http://localhost:5173/legacy/index.html` in a private/incognito browser window (avoids existing localStorage interference).
    2. Dismiss the age-gate dialog (or affirm 18+).
    3. Skip or complete the onboarding wizard.
    4. Create a profile:
       - Name: `Test Subject`
       - Pronouns: `they/them`
       - Emoji: `🌱` (or paste the emoji from the legacy emoji picker — search "seedling")
       - Accent color: `#7c83ff` (in the colour palette picker)
       - Notes: leave empty
    5. From the profile detail view, click "Create result" and name the subject `Test Subject` (or accept the default).
    6. In the category overview screen, enable at least three categories. Recommended: `General connection`, `Emotional intimacy`, `Partnership`. Confirm.
    7. In the questionnaire (list mode is fine), answer at least one item in each enabled category by tapping a scale step.
    8. In ONE of the categories, click the "+ add custom item" affordance and add an item titled `Reading bedtime stories`. Set its scale.
    9. Add a SECOND custom item in any category, e.g. `Surprise hugs`. Set its scale.
    10. In ANY category, hide ONE pre-existing item via the v1.0 "hide" gesture (long-press or the hide button).
    11. Save / leave the questionnaire — auto-save should already have persisted everything.
    12. Open the result view, then open the Share flow.
    13. Enter passphrase: `test-passphrase-fixture-only`
    14. Confirm (re-enter passphrase if v1.0 asks for confirmation).
    15. The armored bundle text appears in a textarea or the share screen. Select ALL of it including the `-----BEGIN RELATIONSHAPE BUNDLE-----` header, the `v1` version line, all base64 body lines, and the `-----END RELATIONSHAPE BUNDLE-----` footer. Copy to clipboard.
    16. Save the copied text exactly to `tests/fixtures/v1-bundle.rshape.txt`. Make sure the file ends with a single trailing newline (matching v1.0's encryptResult output at `public/legacy/js/crypto.js:68`).
    17. Capture the EXPECTED_PAYLOAD. In `public/legacy/js/app.js` find the Share flow's payload assembly (search for `viewShare` around line 2875 or for the call site of `encryptResult`). Add a temporary `console.log('SHARE PAYLOAD:', JSON.stringify(payload, null, 2))` immediately before the `encryptResult(payload, pass)` call. Reload the legacy app's Share view and capture the JSON from the browser console. REMOVE the console.log after capturing.
    18. Create `tests/fixtures/v1-bundle.fixture.ts` with the content shown in the action block below, pasting the captured payload into EXPECTED_PAYLOAD.

    After completing those steps, run from the repo root:

    ```
    test -f tests/fixtures/v1-bundle.rshape.txt && head -1 tests/fixtures/v1-bundle.rshape.txt | grep -c RELATIONSHAPE && test -f tests/fixtures/v1-bundle.fixture.ts && grep -c ARMORED tests/fixtures/v1-bundle.fixture.ts && grep -c PASSPHRASE tests/fixtures/v1-bundle.fixture.ts && grep -c EXPECTED_PAYLOAD tests/fixtures/v1-bundle.fixture.ts && pnpm run typecheck
    ```

    All commands should succeed.

    The content of `tests/fixtures/v1-bundle.fixture.ts` should be:

    ```
    // tests/fixtures/v1-bundle.fixture.ts
    // Captured from /legacy/ on <DATE>. Procedure in fixtures/README.md.
    // Profile: "Test Subject", emoji 🌱, color #7c83ff, pronouns they/them.
    // One result with enabledCategories across 3 categories, 2 custom items, 1 __hidden item.
    // The passphrase is intentionally committed — fixture only, no PII.

    import { readFileSync } from 'node:fs'
    import { fileURLToPath } from 'node:url'
    import { dirname, join } from 'node:path'

    const __dirname_eq = dirname(fileURLToPath(import.meta.url))

    export const ARMORED: string = readFileSync(
      join(__dirname_eq, 'v1-bundle.rshape.txt'),
      'utf-8'
    )

    export const PASSPHRASE = 'test-passphrase-fixture-only'

    // Paste the exact payload the v1.0 Share flow assembled, captured via console.log in app.js.
    // Shape mirrors public/legacy/js/app.js viewShare() payload assembly.
    export const EXPECTED_PAYLOAD = {
      id: '<paste the result id captured from the console.log>',
      name: 'Test Subject',
      pronouns: 'they/them',
      emoji: '🌱',
      color: '#7c83ff',
      subject: 'Test Subject',
      subjectEmoji: '🌱',
      subjectColor: '#7c83ff',
      answers: {
        // ... paste the captured answers object verbatim
      },
      scale: [
        // ... paste the captured scale array verbatim (will be v1.0 DEFAULT_SCALE since the fixture session didn't override)
      ],
      enabledCategories: ['connection', 'intimacy', 'partnership'],  // or whatever you enabled
      askedItems: {},
      version: 1,
      srcVersion: 1,
    } as const
    ```

    Important about the placeholder fields: replace every `<paste ...>` and `// ... paste ...` line with the actual captured values. The plan 04 test does a deep-equal between `decryptResult(ARMORED, PASSPHRASE)` and `EXPECTED_PAYLOAD`, so ANY mismatch will fail the test.
  </how-to-verify>
  <resume-signal>
    Type `approved` when:
    - `tests/fixtures/v1-bundle.rshape.txt` exists with valid PEM armor (first line is the BEGIN header)
    - `tests/fixtures/v1-bundle.fixture.ts` exists and exports ARMORED, PASSPHRASE, EXPECTED_PAYLOAD
    - `pnpm run typecheck` passes

    If you encountered issues (e.g. the legacy Share flow rejected the passphrase, the armored output looked malformed, EXPECTED_PAYLOAD shape was unclear), describe them and we will diagnose.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Legacy app (untrusted UI flow) → fixture file | Captured manually by the user; the user must visually verify the armored text is complete (BEGIN+v1+body+END) and the payload structure matches expectations |
| Fixture .ts wrapper → fs.readFileSync | The TS wrapper reads the .rshape.txt at module load time; Vitest in `node` env supports fs |
| Test ↔ fixture | Plan 04 decrypts ARMORED with PASSPHRASE and asserts equality to EXPECTED_PAYLOAD; mismatch surfaces immediately |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | EXPECTED_PAYLOAD does not match what v1.0 actually assembled — silent test failure with confusing diff | mitigate | Procedure documents a console.log capture step in app.js; the user MUST copy the exact runtime payload, not guess. The plan 04 crypto test will catch any mismatch with a deep-equal assertion |
| T-03-02 | Tampering | Armored bundle is truncated when copy-pasted (missing BEGIN/END or last base64 line) | mitigate | task 3.1 README step 8 explicitly instructs to copy the ENTIRE block. task 3.2 resume signal checks `head -1 ... | grep RELATIONSHAPE` to confirm the header is present |
| T-03-03 | Information Disclosure | Fixture passphrase committed to git becomes a real-world credential | accept | The passphrase is `test-passphrase-fixture-only` — explicitly fixture-only. The bundle decrypts to synthetic "Test Subject" data with no PII. Documented in README §"Fixture passphrase disclosure" |
| T-03-04 | Denial of Service | Captured fixture is non-deterministic; replacing it later breaks plan 04 if EXPECTED_PAYLOAD is not re-captured | mitigate | README documents the regeneration procedure end-to-end; plan 04 imports both ARMORED and EXPECTED_PAYLOAD from the same module so they evolve together |
| T-03-05 | Information Disclosure | Fixture localStorage blob contains realistic PII | n/a | Synthetic data only (name "Test Subject", made-up category answers) — no real personal data |
</threat_model>

<verification>
After both tasks complete:

```
# All fixture files present
ls -la tests/fixtures/v1-bundle.rshape.txt tests/fixtures/v1-bundle.fixture.ts tests/fixtures/v1-localstorage.fixture.ts tests/fixtures/README.md tests/setup.ts

# Armor header preserved
head -1 tests/fixtures/v1-bundle.rshape.txt | grep -c RELATIONSHAPE

# Armor footer preserved
tail -2 tests/fixtures/v1-bundle.rshape.txt | grep -c "END RELATIONSHAPE"

# TS fixtures compile under D-15 strict
pnpm run typecheck
```
</verification>

<success_criteria>
- `tests/fixtures/v1-bundle.rshape.txt` exists with a valid PEM armor structure (header, `v1` line, base64 body, footer)
- `tests/fixtures/v1-bundle.fixture.ts` exports `ARMORED` (string), `PASSPHRASE` (the documented fixture passphrase), and `EXPECTED_PAYLOAD` (the exact runtime-captured payload object)
- `tests/fixtures/v1-localstorage.fixture.ts` exports `V1_LOCALSTORAGE_BLOB` (string) and `V1_LOCALSTORAGE_PARSED` (object), with shape matching `public/legacy/js/storage.js` defaults and `public/legacy/js/data.js` DEFAULT_SCALE
- `tests/fixtures/README.md` documents the regeneration procedure step-by-step including the exact fixture passphrase
- `tests/setup.ts` exists as a valid TypeScript module
- `pnpm run typecheck` exits 0 (D-15 strict mode passes on all fixtures)
- Plans 04 and 06 can import these fixtures with no further capture step required
</success_criteria>

<output>
After completion, create `.planning/phases/01-skeleton/01-03-SUMMARY.md` documenting:
- The exact byte size of `v1-bundle.rshape.txt` (typically ~1-3 KB depending on payload size)
- The capture date
- A snippet of the EXPECTED_PAYLOAD top-level keys (to confirm the human captured the right shape)
- Confirmation that the passphrase committed to README matches the one in v1-bundle.fixture.ts (must be identical)
- Pointer to plans 04 (crypto), 05 (data+i18n), 06 (storage), 07 (design) as the next steps in Wave 4
</output>
