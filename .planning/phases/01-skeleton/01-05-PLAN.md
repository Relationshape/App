---
phase: 01-skeleton
plan: 05
type: execute
wave: 4
depends_on:
  - "01-02"
files_modified:
  - src/lib/data/data.ts
  - src/lib/data/types.ts
  - src/lib/data/index.ts
  - src/lib/data/__tests__/data.test.ts
  - src/lib/i18n/en.ts
  - src/lib/i18n/de.ts
  - src/lib/i18n/i18n.ts
  - src/lib/i18n/index.ts
  - src/lib/i18n/__tests__/i18n.test.ts
autonomous: true
requirements:
  - CORE-05
  - CORE-06
tags:
  - data
  - i18n
  - port
  - typescript
  - as-const
  - translations

must_haves:
  truths:
    - "src/lib/data/data.ts exports CATEGORIES, DEFAULT_SCALE, SPIDER_AXES, CATEGORY_GROUPS, FILE_FORMAT — all as const, content-frozen, byte-identical to public/legacy/js/data.js values (CORE-05)"
    - "src/lib/data/types.ts derives CategoryId, ScaleStep, Lang, SpiderAxisId from the as const arrays — D-13 type-derivation pattern"
    - "src/lib/i18n/en.ts exports EN as const with all 304 v1.0 EN translation keys verbatim from public/legacy/js/i18n.js TRANSLATIONS.en"
    - "src/lib/i18n/de.ts exports DE: Record<TranslationKey, string> — TypeScript enforces every EN key has a DE counterpart (D-13 compile-time check)"
    - "src/lib/i18n/i18n.ts exports t(key, vars?), getLang(), setLang(lang), getLocalizedDefaultScale(englishDefault) — semantic parity with v1.0's i18n.js"
    - "Custom t() port (~50 lines) — NOT react-i18next. Per D-12"
    - "Language detection mirrors v1.0 (D-14): localStorage settings.lang prefix → navigator.language → 'en'"
    - "EN and DE have identical key sets — runtime test asserts Object.keys(EN).sort() equals Object.keys(DE).sort()"
    - "i18n key count is 304 (v1.0 baseline per RESEARCH.md)"
    - "t() supports {var} interpolation: t('seeded_toast', { name: 'Alice' }) substitutes correctly"
    - "getLocalizedDefaultScale returns DEFAULT_SCALE_DE when _lang === 'de', the supplied English default otherwise"
  artifacts:
    - path: "src/lib/data/data.ts"
      provides: "Typed const ports of v1.0 CATEGORIES, DEFAULT_SCALE, SPIDER_AXES, CATEGORY_GROUPS, FILE_FORMAT — content frozen"
      min_lines: 800
      exports:
        - "CATEGORIES"
        - "DEFAULT_SCALE"
        - "SPIDER_AXES"
        - "CATEGORY_GROUPS"
        - "FILE_FORMAT"
    - path: "src/lib/data/types.ts"
      provides: "Type unions derived from the as const arrays"
      exports:
        - "CategoryId"
        - "ScaleStep"
        - "Lang"
        - "SpiderAxisId"
    - path: "src/lib/data/index.ts"
      provides: "Barrel re-export"
    - path: "src/lib/i18n/en.ts"
      provides: "EN translation map as const (304 keys verbatim from v1.0)"
      exports:
        - "EN"
        - "TranslationKey"
    - path: "src/lib/i18n/de.ts"
      provides: "DE translation map constrained to Record<TranslationKey, string>"
      exports:
        - "DE"
    - path: "src/lib/i18n/i18n.ts"
      provides: "t(), getLang(), setLang(), getLocalizedDefaultScale() — semantic port of v1.0 i18n.js"
      exports:
        - "t"
        - "getLang"
        - "setLang"
        - "getLocalizedDefaultScale"
        - "availableLangs"
        - "DEFAULT_SCALE_DE"
    - path: "src/lib/i18n/index.ts"
      provides: "Barrel re-export"
    - path: "src/lib/data/__tests__/data.test.ts"
      provides: "Shape sanity assertions for CORE-05 — category count, no duplicate IDs, scale length === 7"
    - path: "src/lib/i18n/__tests__/i18n.test.ts"
      provides: "Key parity, key count, interpolation, localized scale (CORE-06)"
  key_links:
    - from: "src/lib/i18n/de.ts"
      to: "src/lib/i18n/en.ts"
      via: "import type { TranslationKey } from './en'; constrain DE to Record<TranslationKey, string>"
      pattern: "Record<TranslationKey"
    - from: "src/lib/i18n/i18n.ts"
      to: "src/lib/i18n/en.ts + de.ts"
      via: "import { EN } from './en'; import { DE } from './de'"
      pattern: "from './en'"
    - from: "src/lib/data/types.ts"
      to: "src/lib/data/data.ts"
      via: "type CategoryId = typeof CATEGORIES[number]['id']"
      pattern: "typeof CATEGORIES\\[number\\]"
---

<objective>
Port two pure-content modules from `public/legacy/js/` to TypeScript with byte-for-byte content parity:
1. `data.js` (870 lines, CORE-05) → `src/lib/data/{data,types,index}.ts` with `as const` arrays so types are derived at compile time.
2. `i18n.js` (847 lines, CORE-06) → `src/lib/i18n/{en,de,i18n,index}.ts` with a derived `TranslationKey` type that catches missing translations at compile time (D-13 — solves v1.0's "missing key silently falls back to raw key" failure mode).

Both ports are LARGE files (data.js ~28 KB, i18n.js ~50 KB) but mechanically simple — copy content verbatim, add `as const`, add types, derive unions. The runtime behaviour of `t()`, `getLang()`, `setLang()`, `getLocalizedDefaultScale()` is preserved exactly.

Purpose: CORE-05 freezes the questionnaire content for v2.0 (the questionnaire schema is upstream-licensed CC BY-NC 4.0 and not part of the migration). CORE-06 closes the "missing translation silently falls back" gap by using TypeScript's structural typing — `DE: Record<TranslationKey, string>` is a compile error if any EN key is missing from DE.

Output: 9 files (data: 3 source + 1 test; i18n: 4 source + 1 test). All Vitest tests pass: data.test.ts asserts category count + no-duplicate-IDs + scale length; i18n.test.ts asserts EN/DE key parity + 304-key count + interpolation + getLocalizedDefaultScale.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-skeleton/01-CONTEXT.md
@.planning/phases/01-skeleton/01-RESEARCH.md
@.planning/phases/01-skeleton/01-PATTERNS.md
@public/legacy/js/data.js
@public/legacy/js/i18n.js

Verified line/key counts (from RESEARCH.md):
- data.js: 870 lines, 5 named exports (CATEGORIES with ~30 entries, DEFAULT_SCALE 7 steps, SPIDER_AXES, CATEGORY_GROUPS, FILE_FORMAT)
- i18n.js: 847 lines, 304 EN keys + 304 DE keys (key counts match)
</context>

<tasks>

<task type="auto">
  <name>Task 5.1: Port src/lib/data/{data,types,index}.ts from public/legacy/js/data.js (CORE-05) with content-frozen as const exports and derived types</name>
  <files>src/lib/data/data.ts, src/lib/data/types.ts, src/lib/data/index.ts, src/lib/data/__tests__/data.test.ts</files>
  <read_first>
    - public/legacy/js/data.js (FULL FILE — 870 lines; this is the source of truth)
    - .planning/phases/01-skeleton/01-PATTERNS.md (section `src/lib/data/data.ts` — type derivation pattern)
    - .planning/phases/01-skeleton/01-CONTEXT.md (D-21, locked content)
  </read_first>
  <action>
    Create `src/lib/data/data.ts` by copying the ENTIRE content of `public/legacy/js/data.js` into a new TypeScript file with three mechanical edits:

    1. Add `as const` to every top-level array export so TypeScript can derive literal types:
       - `export const DEFAULT_SCALE = [...] as const`
       - `export const CATEGORIES = [...] as const`
       - `export const SPIDER_AXES = [...] as const`
       - `export const CATEGORY_GROUPS = {...} as const` (if object) or `[...] as const` (if array — read v1.0 to confirm)
       - `export const FILE_FORMAT = { magic: 'RSHAPE1', version: 1 } as const`

    2. Remove the `.js` extension hint from any internal imports (data.js is self-contained per ARCHITECTURE.md — should be no imports — but if there are any `from './something.js'`, change to `from './something'`).

    3. NO content changes. NO key renames. NO type annotations on the inline values (the literal-typed values come from `as const`). Categories, item names, blurbs, colors, emoji — all byte-identical to v1.0.

    Critical do-NOTs:
    - Do NOT add `id`, `title`, `de`, `icon`, `color`, `blurb`, `deBlurb`, `items`, `deItems` field annotations — the `as const` derives the structural type
    - Do NOT modify any string literal — these are user-facing questionnaire content
    - Do NOT alphabetise or reorder array entries — preserve the v1.0 order

    Write `src/lib/data/types.ts` with the derived unions:

    ```
    // src/lib/data/types.ts
    // Type derivations from the as const exports in data.ts.
    // CORE-05 — content frozen, types derived at compile time.

    import type { CATEGORIES, DEFAULT_SCALE, SPIDER_AXES } from './data'

    export type CategoryId = (typeof CATEGORIES)[number]['id']
    export type ScaleStep = (typeof DEFAULT_SCALE)[number]
    export type SpiderAxisId = (typeof SPIDER_AXES)[number]
    export type Lang = 'en' | 'de'

    // Mutable scale step shape (for migration / user-edited scales) — same field set as DEFAULT_SCALE entries
    export interface MutableScaleStep {
      key: string
      label: string
      short: string
      value: number
      color: string
      description: string
    }
    ```

    Write `src/lib/data/index.ts`:

    ```
    export {
      CATEGORIES,
      DEFAULT_SCALE,
      SPIDER_AXES,
      CATEGORY_GROUPS,
      FILE_FORMAT,
    } from './data'
    export type { CategoryId, ScaleStep, SpiderAxisId, Lang, MutableScaleStep } from './types'
    ```

    Write `src/lib/data/__tests__/data.test.ts`:

    ```
    // src/lib/data/__tests__/data.test.ts
    // CORE-05: shape sanity for the ported data module.
    import { describe, it, expect } from 'vitest'
    import {
      CATEGORIES,
      DEFAULT_SCALE,
      SPIDER_AXES,
      FILE_FORMAT,
    } from '../data'

    describe('data.ts shape sanity (CORE-05)', () => {
      it('DEFAULT_SCALE has 7 steps from "no" (value 0) to "need" (value 6)', () => {
        expect(DEFAULT_SCALE).toHaveLength(7)
        expect(DEFAULT_SCALE[0]?.key).toBe('no')
        expect(DEFAULT_SCALE[0]?.value).toBe(0)
        expect(DEFAULT_SCALE[6]?.key).toBe('need')
        expect(DEFAULT_SCALE[6]?.value).toBe(6)
      })

      it('every DEFAULT_SCALE step has all six required fields', () => {
        for (const step of DEFAULT_SCALE) {
          expect(step.key).toBeTypeOf('string')
          expect(step.label).toBeTypeOf('string')
          expect(step.short).toBeTypeOf('string')
          expect(step.value).toBeTypeOf('number')
          expect(step.color).toMatch(/^#[0-9a-fA-F]{6}$/)
          expect(step.description).toBeTypeOf('string')
        }
      })

      it('CATEGORIES is a non-empty array with unique ids', () => {
        expect(CATEGORIES.length).toBeGreaterThan(0)
        const ids = CATEGORIES.map((c) => c.id)
        const uniqueIds = new Set(ids)
        expect(uniqueIds.size).toBe(ids.length)
      })

      it('every CATEGORIES entry has id, title, de, icon, color, items, deItems fields', () => {
        for (const cat of CATEGORIES) {
          expect(cat.id).toBeTypeOf('string')
          expect(cat.title).toBeTypeOf('string')
          expect(cat.de).toBeTypeOf('string')
          expect(cat.icon).toBeTypeOf('string')
          expect(cat.color).toMatch(/^#[0-9a-fA-F]{6}$/)
          expect(Array.isArray(cat.items)).toBe(true)
          expect(Array.isArray(cat.deItems)).toBe(true)
          expect(cat.items.length).toBe(cat.deItems.length)
        }
      })

      it('SPIDER_AXES references only valid CategoryIds', () => {
        const ids = new Set(CATEGORIES.map((c) => c.id))
        for (const axis of SPIDER_AXES) {
          expect(ids.has(axis as string)).toBe(true)
        }
      })

      it('FILE_FORMAT is the locked v1 envelope shape', () => {
        expect(FILE_FORMAT.magic).toBe('RSHAPE1')
        expect(FILE_FORMAT.version).toBe(1)
      })
    })
    ```

    Run the test suite:

    ```
    pnpm run typecheck
    pnpm run test -- --run src/lib/data/__tests__/data.test.ts
    ```

    Both must exit 0. If `pnpm run typecheck` fails due to `noUncheckedIndexedAccess` (D-15) on `DEFAULT_SCALE[0]?.key`, the optional chaining `?.` is the right escape hatch — the test already uses it.
  </action>
  <verify>
    <automated>test -f src/lib/data/data.ts && test -f src/lib/data/types.ts && test -f src/lib/data/index.ts && test -f src/lib/data/__tests__/data.test.ts && grep -c "as const" src/lib/data/data.ts && grep -c "export const CATEGORIES" src/lib/data/data.ts && grep -c "export const DEFAULT_SCALE" src/lib/data/data.ts && grep -c "export const SPIDER_AXES" src/lib/data/data.ts && grep -c "export const FILE_FORMAT" src/lib/data/data.ts && grep -c "RSHAPE1" src/lib/data/data.ts && grep -c "CategoryId" src/lib/data/types.ts && grep -c "ScaleStep" src/lib/data/types.ts && pnpm run typecheck && pnpm run test -- --run src/lib/data/__tests__/data.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/lib/data/data.ts` returns 0
    - `test -f src/lib/data/types.ts` returns 0
    - `test -f src/lib/data/index.ts` returns 0
    - `test -f src/lib/data/__tests__/data.test.ts` returns 0
    - `wc -l src/lib/data/data.ts` returns at least 800 (port preserves the legacy file size; legacy is 870 lines)
    - `grep -c 'as const' src/lib/data/data.ts` returns at least 5 (one per exported array/object)
    - `grep -c 'export const CATEGORIES' src/lib/data/data.ts` returns 1
    - `grep -c 'export const DEFAULT_SCALE' src/lib/data/data.ts` returns 1
    - `grep -c 'export const SPIDER_AXES' src/lib/data/data.ts` returns 1
    - `grep -c 'export const FILE_FORMAT' src/lib/data/data.ts` returns 1
    - `grep -c "magic: 'RSHAPE1'" src/lib/data/data.ts` returns 1
    - `grep -c "version: 1" src/lib/data/data.ts` returns at least 1
    - `grep -c 'export type CategoryId' src/lib/data/types.ts` returns 1
    - `grep -c 'export type ScaleStep' src/lib/data/types.ts` returns 1
    - `grep -c 'export type Lang' src/lib/data/types.ts` returns 1
    - `grep -c "typeof CATEGORIES\\[number\\]\\['id'\\]" src/lib/data/types.ts` returns 1
    - `pnpm run typecheck` exits 0
    - `pnpm run test -- --run src/lib/data/__tests__/data.test.ts` exits 0 with all assertions passing
  </acceptance_criteria>
  <done>
    `src/lib/data/data.ts` mirrors `public/legacy/js/data.js` content with `as const` on every exported array/object. `src/lib/data/types.ts` derives CategoryId, ScaleStep, SpiderAxisId, Lang. The shape-sanity test passes 6 assertions confirming DEFAULT_SCALE has 7 steps, CATEGORIES has unique IDs and the required field set, SPIDER_AXES references valid IDs, FILE_FORMAT magic = 'RSHAPE1' / version = 1.
  </done>
</task>

<task type="auto">
  <name>Task 5.2: Port src/lib/i18n/{en,de,i18n,index}.ts from public/legacy/js/i18n.js (CORE-06) with typed translations + custom t() + 304-key parity test</name>
  <files>src/lib/i18n/en.ts, src/lib/i18n/de.ts, src/lib/i18n/i18n.ts, src/lib/i18n/index.ts, src/lib/i18n/__tests__/i18n.test.ts</files>
  <read_first>
    - public/legacy/js/i18n.js (FULL FILE — 847 lines; source of truth for both EN and DE keys + t() semantics)
    - src/lib/data/data.ts (the data port from task 5.1 — i18n.ts may import DEFAULT_SCALE for getLocalizedDefaultScale's fallback)
    - src/lib/data/types.ts (Lang type)
    - .planning/phases/01-skeleton/01-PATTERNS.md (section `src/lib/i18n/en.ts` through `src/lib/i18n/i18n.ts`)
    - .planning/phases/01-skeleton/01-CONTEXT.md (D-12, D-13, D-14)
    - .planning/phases/01-skeleton/01-RESEARCH.md (Pattern 5 "Custom typed i18n" + "Verified i18n key parity")
  </read_first>
  <action>
    Open `public/legacy/js/i18n.js` and locate:
    - The `TRANSLATIONS` object opening (~line 5)
    - The `en:` block start and end (~lines 6-396)
    - The `de:` block start and end (~lines 397-785)
    - `DEFAULT_SCALE_DE` (~lines 788-796)
    - `detectLanguage` (~lines 798-811)
    - `_lang` declaration (~line 811)
    - `getLang` (~line 813)
    - `setLang` (~line 815)
    - `availableLangs` (~line 836)
    - `t` (~line 827)
    - `getLocalizedDefaultScale` (~line 843)

    Write `src/lib/i18n/en.ts`:

    ```
    // src/lib/i18n/en.ts
    // Ported verbatim from public/legacy/js/i18n.js TRANSLATIONS.en (~lines 6-396).
    // CORE-06: 304 keys, content-frozen.
    // Source of truth for TranslationKey type — DE is constrained to Record<TranslationKey, string>.

    export const EN = {
      // <paste every key from TRANSLATIONS.en here, verbatim, preserving section header comments>
      // e.g.:
      // // Nav
      // nav_profiles: '👤 Profiles',
      // nav_import: '📥 Import/Export',
      // ... all 304 keys ...
    } as const

    export type TranslationKey = keyof typeof EN
    ```

    Practical instruction for the executor: copy the entire `en: { ... }` literal object body from `public/legacy/js/i18n.js` into the `EN = { ... }` literal. PRESERVE all section-header comments (// Nav, // Home / Welcome, etc.) so future diffs against v1.0 stay readable. PRESERVE all `\n` escape sequences inside string values verbatim — they are part of the rendered output. Convert v1.0's double-quoted strings to single quotes (Prettier will do this anyway). Add a trailing `as const` after the closing `}`.

    Write `src/lib/i18n/de.ts`:

    ```
    // src/lib/i18n/de.ts
    // Ported verbatim from public/legacy/js/i18n.js TRANSLATIONS.de (~lines 397-785).
    // CORE-06: 304 keys, key-for-key parity with EN.
    // D-13: type-constrained to Record<TranslationKey, string> — TS compile-fails on any missing key.

    import type { TranslationKey } from './en'

    export const DE: Record<TranslationKey, string> = {
      // <paste every key from TRANSLATIONS.de here, verbatim>
      // e.g.:
      // nav_profiles: '👤 Profile',
      // nav_import: '📥 Import/Export',
      // ... all 304 keys ...
    }
    ```

    Same instruction: copy the entire `de: { ... }` literal body, preserve comments and escape sequences. If the TS compiler complains about a missing key, that's the D-13 type-safety win — fix the omission against `public/legacy/js/i18n.js`.

    Write `src/lib/i18n/i18n.ts`:

    ```
    // src/lib/i18n/i18n.ts
    // Custom 50-line t() port per D-12. NOT react-i18next.
    // Mirrors semantics of public/legacy/js/i18n.js lines 798-846.

    import { DEFAULT_SCALE } from '@/lib/data/data'
    import type { Lang, MutableScaleStep } from '@/lib/data/types'
    import { EN, type TranslationKey } from './en'
    import { DE } from './de'

    const TRANSLATIONS = { en: EN, de: DE } as const

    // German DEFAULT_SCALE — ported verbatim from public/legacy/js/i18n.js ~lines 788-796.
    export const DEFAULT_SCALE_DE: readonly MutableScaleStep[] = [
      // <paste DEFAULT_SCALE_DE from legacy i18n.js — 7 entries, key-for-key>
    ] as const

    function detectLanguage(): Lang {
      // Mirror v1.0: localStorage settings.lang → navigator.language → 'en'
      try {
        if (typeof localStorage !== 'undefined') {
          const raw = localStorage.getItem('relationshape.v1')
          if (raw) {
            const data = JSON.parse(raw) as { settings?: { lang?: string } }
            const stored = data.settings?.lang
            if (stored === 'en' || stored === 'de') return stored
          }
        }
      } catch {
        // ignore
      }
      if (typeof navigator !== 'undefined') {
        const browser = (navigator.language || 'en').split('-')[0]?.toLowerCase() ?? 'en'
        if (browser === 'de') return 'de'
      }
      return 'en'
    }

    let _lang: Lang = detectLanguage()

    export function getLang(): Lang {
      return _lang
    }

    export function setLang(lang: Lang): void {
      if (lang !== 'en' && lang !== 'de') return
      _lang = lang
      // Persistence to localStorage is the Zustand store's responsibility (plan 06).
      // This function only updates the module-level lang.
    }

    export function availableLangs(): readonly Lang[] {
      return ['en', 'de'] as const
    }

    export function t(
      key: TranslationKey,
      vars: Record<string, string | number> = {},
    ): string {
      const dict = TRANSLATIONS[_lang]
      let str: string = dict[key] ?? EN[key] ?? (key as string)
      for (const [k, v] of Object.entries(vars)) {
        str = str.replaceAll(`{${k}}`, String(v))
      }
      return str
    }

    export function getLocalizedDefaultScale(
      englishDefault: readonly MutableScaleStep[] = DEFAULT_SCALE as readonly MutableScaleStep[],
    ): readonly MutableScaleStep[] {
      if (_lang === 'de') return DEFAULT_SCALE_DE
      return englishDefault
    }
    ```

    Notes:
    - `noUncheckedIndexedAccess` (D-15): `browser.split('-')[0]` is `string | undefined`. The `?? 'en'` fallback handles undefined.
    - `replaceAll` with template-literal pattern is the v1.0 substitution. Available in ES2021+; D-15 `target: 'ES2022'` supports it.
    - `getLocalizedDefaultScale` accepts an optional englishDefault for parity with v1.0 (which passes the store's potentially-customised scale).
    - DEFAULT_SCALE_DE is exported from this module (NOT from `data.ts`) per PATTERNS.md note — `data.ts` is English-source-of-truth; localization lives in `i18n/`.

    Write `src/lib/i18n/index.ts`:

    ```
    export { EN, type TranslationKey } from './en'
    export { DE } from './de'
    export {
      t,
      getLang,
      setLang,
      availableLangs,
      getLocalizedDefaultScale,
      DEFAULT_SCALE_DE,
    } from './i18n'
    ```

    Write `src/lib/i18n/__tests__/i18n.test.ts`:

    ```
    // src/lib/i18n/__tests__/i18n.test.ts
    // CORE-06: EN+DE key parity, key count, interpolation, localized scale.
    import { describe, it, expect, beforeEach } from 'vitest'
    import { EN } from '../en'
    import { DE } from '../de'
    import { t, setLang, getLang, getLocalizedDefaultScale, DEFAULT_SCALE_DE } from '../i18n'

    describe('i18n (CORE-06)', () => {
      beforeEach(() => {
        setLang('en')
      })

      it('EN and DE have identical key sets', () => {
        const enKeys = Object.keys(EN).sort()
        const deKeys = Object.keys(DE).sort()
        expect(deKeys).toEqual(enKeys)
      })

      it('EN key count matches v1.0 baseline (304 keys)', () => {
        expect(Object.keys(EN).length).toBe(304)
      })

      it('t() resolves an EN key in EN mode', () => {
        setLang('en')
        // welcome_title is a known v1.0 key — value should be "Relationshapes"
        expect(t('welcome_title')).toBe('Relationshapes')
      })

      it('t() resolves a DE key in DE mode and differs from the EN value', () => {
        setLang('de')
        expect(getLang()).toBe('de')
        const deVal = t('welcome_title')
        setLang('en')
        const enVal = t('welcome_title')
        // The exact DE string for welcome_title is whatever v1.0 has (likely "Beziehungsformen" or similar)
        expect(deVal).not.toBe(enVal)
      })

      it('t() supports {var} interpolation', () => {
        setLang('en')
        // seeded_toast in v1.0 uses {name} placeholder
        // The actual key may vary — replace with any known interpolated key, e.g. imported_versioned_toast uses {n}
        const result = t('seeded_toast' as keyof typeof EN, { name: 'Alice' })
        expect(result).toContain('Alice')
      })

      it('getLocalizedDefaultScale returns DEFAULT_SCALE_DE when lang is de', () => {
        setLang('de')
        const scale = getLocalizedDefaultScale()
        expect(scale).toBe(DEFAULT_SCALE_DE)
        expect(scale.length).toBe(7)
      })

      it('getLocalizedDefaultScale returns the supplied English default when lang is en', () => {
        setLang('en')
        const customDefault = [{ key: 'x', label: 'X', short: 'X', value: 0, color: '#000000', description: '' }]
        const scale = getLocalizedDefaultScale(customDefault)
        expect(scale).toBe(customDefault)
      })

      it('setLang ignores invalid lang strings', () => {
        setLang('en')
        // @ts-expect-error testing runtime invalid input
        setLang('fr')
        expect(getLang()).toBe('en')
      })
    })
    ```

    Notes on the test:
    - The `seeded_toast` test depends on v1.0 having a key by that name with `{name}` placeholder. If `public/legacy/js/i18n.js` uses a different key name, substitute it. The executor must pick a real v1.0 key that includes a `{var}` placeholder — search `public/legacy/js/i18n.js` for `/{[a-z]+}/` to find candidates.
    - The DE value for welcome_title is whatever v1.0 says — the test only asserts that it differs from EN, not the exact German string. If v1.0 happens to have identical EN+DE for that key, swap to another key.
    - `setLang('fr' as Lang)` triggers a `@ts-expect-error` because Lang is `'en' | 'de'` — the test confirms runtime guard.

    Run:

    ```
    pnpm run typecheck
    pnpm run lint
    pnpm run test -- --run src/lib/i18n/__tests__/i18n.test.ts
    ```

    All must exit 0. If `pnpm run typecheck` fails because DE is missing some keys, that's the D-13 win — open `public/legacy/js/i18n.js` and copy the missing entries.

    Critical do-NOTs:
    - Do NOT use `react-i18next` — D-12 forbids it
    - Do NOT add additional translation keys not in v1.0 — content frozen
    - Do NOT change the `{var}` placeholder syntax to `${var}` or `:var` — v1.0 uses `{var}`
    - Do NOT touch localStorage from `setLang` — that's the Zustand store's responsibility (plan 06)
    - Do NOT change the language detection chain order (localStorage → navigator → 'en') — D-14 lock
  </action>
  <verify>
    <automated>test -f src/lib/i18n/en.ts && test -f src/lib/i18n/de.ts && test -f src/lib/i18n/i18n.ts && test -f src/lib/i18n/index.ts && test -f src/lib/i18n/__tests__/i18n.test.ts && grep -c "export const EN" src/lib/i18n/en.ts && grep -c "as const" src/lib/i18n/en.ts && grep -c "export type TranslationKey" src/lib/i18n/en.ts && grep -c "Record<TranslationKey, string>" src/lib/i18n/de.ts && grep -c "export const DE" src/lib/i18n/de.ts && grep -c "export function t" src/lib/i18n/i18n.ts && grep -c "export function getLang" src/lib/i18n/i18n.ts && grep -c "export function setLang" src/lib/i18n/i18n.ts && grep -c "export function getLocalizedDefaultScale" src/lib/i18n/i18n.ts && grep -c "DEFAULT_SCALE_DE" src/lib/i18n/i18n.ts && grep -c "detectLanguage" src/lib/i18n/i18n.ts && grep -c "navigator.language" src/lib/i18n/i18n.ts && grep -c "relationshape.v1" src/lib/i18n/i18n.ts && pnpm run typecheck && pnpm run lint && pnpm run test -- --run src/lib/i18n/__tests__/i18n.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/lib/i18n/en.ts` returns 0
    - `test -f src/lib/i18n/de.ts` returns 0
    - `test -f src/lib/i18n/i18n.ts` returns 0
    - `test -f src/lib/i18n/index.ts` returns 0
    - `test -f src/lib/i18n/__tests__/i18n.test.ts` returns 0
    - `grep -c 'export const EN' src/lib/i18n/en.ts` returns 1
    - `grep -c 'as const' src/lib/i18n/en.ts` returns at least 1
    - `grep -c 'export type TranslationKey' src/lib/i18n/en.ts` returns 1
    - `grep -c 'typeof EN' src/lib/i18n/en.ts` returns 1 (TranslationKey = keyof typeof EN)
    - `grep -c 'Record<TranslationKey, string>' src/lib/i18n/de.ts` returns 1
    - `grep -c 'export const DE' src/lib/i18n/de.ts` returns 1
    - `grep -c 'export function t' src/lib/i18n/i18n.ts` returns 1
    - `grep -c 'export function getLang' src/lib/i18n/i18n.ts` returns 1
    - `grep -c 'export function setLang' src/lib/i18n/i18n.ts` returns 1
    - `grep -c 'export function getLocalizedDefaultScale' src/lib/i18n/i18n.ts` returns 1
    - `grep -c 'DEFAULT_SCALE_DE' src/lib/i18n/i18n.ts` returns at least 2 (declaration + return)
    - `grep -c "'relationshape.v1'" src/lib/i18n/i18n.ts` returns 1 (legacy localStorage key for lang detection)
    - `grep -c 'navigator.language' src/lib/i18n/i18n.ts` returns 1 (browser detection)
    - `grep -c 'react-i18next' src/lib/i18n/i18n.ts` returns 0 (D-12 — no library import)
    - `pnpm run typecheck` exits 0 (D-13 — DE has every EN key)
    - `pnpm run lint` exits 0
    - `pnpm run test -- --run src/lib/i18n/__tests__/i18n.test.ts` exits 0 with all assertions passing — INCLUDING the assertion that key count is 304
  </acceptance_criteria>
  <done>
    Translation modules `en.ts`, `de.ts`, `i18n.ts`, `index.ts` exist; EN has 304 keys verbatim from v1.0; DE is type-constrained to `Record<TranslationKey, string>` and contains 304 keys; `t()` resolves keys, supports `{var}` interpolation, falls back to EN then to raw key; `getLang`/`setLang` track the module-level lang; `getLocalizedDefaultScale` returns DEFAULT_SCALE_DE in DE mode. The 8-assertion Vitest test passes (key parity, key count 304, EN/DE resolution, interpolation, localized scale switching, runtime guard against invalid lang).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-supplied string into t() interpolation vars | v1.0 uses `replaceAll`; placeholder values are STRING-coerced via `String(v)`; not HTML-escaped here — escaping happens at the DOM-insertion site (Phase 2 concern) |
| localStorage["relationshape.v1"].settings.lang → setLang validation | i18n.ts only accepts 'en' or 'de'; unrecognised values fall through to 'en' default |
| EN → DE key parity | Enforced at compile time via D-13 `Record<TranslationKey, string>`; runtime test re-verifies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | CATEGORIES content silently drifts from v1.0 (a renamed item, a changed colour) — questionnaire schema mismatch when comparing v1 ↔ v2 results | mitigate | CORE-05 content-frozen; data.test.ts asserts category count + no duplicate IDs + items.length === deItems.length; any port omission surfaces at test time |
| T-05-02 | Information Disclosure | t() interpolation accidentally renders user-supplied HTML | accept | t() returns plain strings; HTML escaping is the responsibility of the rendering site (Phase 2 concern — RESULT-07). v1.0 behaves identically; not a Phase 1 regression |
| T-05-03 | Spoofing | A DE key is missing → t() silently returns the raw key (v1.0 failure mode) | mitigate | D-13 `Record<TranslationKey, string>` makes the missing key a compile error; runtime test re-confirms |
| T-05-04 | Denial of Service | t() in a hot path with thousands of substitutions per render | accept | v1.0 has the same `replaceAll` per-key implementation; Phase 1's UI surface (DesignSystem page) calls t() ~10-50 times per render; not a bottleneck |
| T-05-05 | Elevation of Privilege | A malicious translation string contains a substitution that overrides system text | accept | EN+DE source files are committed code, not user input — not an attacker vector. The substitution pattern is `{name}` style, not eval |
</threat_model>

<verification>
After both tasks complete:

```
# All artifact files present
ls -la src/lib/data/ src/lib/i18n/

# Tests all pass
pnpm run test -- --run src/lib/data/__tests__/data.test.ts
pnpm run test -- --run src/lib/i18n/__tests__/i18n.test.ts

# Full typecheck/lint
pnpm run typecheck
pnpm run lint
```
</verification>

<success_criteria>
- `src/lib/data/data.ts` is a content-frozen port with `as const` on every export; 5 named exports (CATEGORIES, DEFAULT_SCALE, SPIDER_AXES, CATEGORY_GROUPS, FILE_FORMAT)
- `src/lib/data/types.ts` derives CategoryId, ScaleStep, SpiderAxisId, Lang from the `as const` arrays
- `src/lib/i18n/en.ts` exports EN as const + TranslationKey type; 304 keys
- `src/lib/i18n/de.ts` is type-constrained to `Record<TranslationKey, string>`; 304 keys (compile-checked)
- `src/lib/i18n/i18n.ts` exports t, getLang, setLang, availableLangs, getLocalizedDefaultScale, DEFAULT_SCALE_DE — custom port per D-12 (no react-i18next)
- All barrels exist and re-export the public surface
- data.test.ts passes 6 assertions
- i18n.test.ts passes 8 assertions, including the 304-key count and EN/DE key-set parity
- CORE-05 and CORE-06 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-skeleton/01-05-SUMMARY.md` documenting:
- `wc -l` for `data.ts`, `en.ts`, `de.ts` (should be 800+, 300+, 300+ respectively)
- Object.keys count for EN and DE (must both be exactly 304)
- Confirmation that data.test.ts and i18n.test.ts pass
- Pointer to plans 06/07 as parallel Wave 4 plans
</output>
