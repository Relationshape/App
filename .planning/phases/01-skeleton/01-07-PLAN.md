---
phase: 01-skeleton
plan: 07
type: execute
wave: 4
depends_on:
  - "01-02"
files_modified:
  - src/styles/theme.css
  - src/styles/animations.css
  - src/styles/fonts.css
  - src/styles/globals.css
  - src/main.tsx
  - src/styles/__tests__/theme-tokens.test.ts
  - src/styles/__tests__/animations.test.ts
autonomous: true
requirements:
  - DESIGN-01
  - DESIGN-02
  - DESIGN-03
  - DESIGN-04
tags:
  - design
  - tailwind
  - tokens
  - fonts
  - animations
  - reduced-motion
  - celestial-map

must_haves:
  truths:
    - "src/styles/theme.css declares a Tailwind v4 @theme block mirroring every :root token from public/legacy/css/style.css with renamed prefixes (Pitfall 1): --bg → --color-bg, --primary → --color-primary, --glow → --shadow-glow, etc. Values byte-identical to v1.0 (DESIGN-01)"
    - "Dark default + light override via [data-theme='light'] selector + auto mode via prefers-color-scheme media query — preserving v1.0 dark-default semantic (D-19)"
    - "src/styles/fonts.css (optional) OR src/main.tsx imports @fontsource-variable/dm-sans and @fontsource-variable/playfair-display — zero references to fonts.googleapis.com or fonts.gstatic.com in src/ or dist/ (DESIGN-02)"
    - "src/styles/animations.css contains all 8 @keyframes from public/legacy/css/additions.css verbatim: heroBlobPulse, holoOrbDrift, holoBtnSpin, holoIconSpin, holoUnderlineSlide, iridShift, bgPulse, silkShift (DESIGN-03)"
    - "src/styles/animations.css contains a @media (prefers-reduced-motion: reduce) block that DISABLES every animation (animation: none !important on the keyframe targets + universal duration zeroing) — D-10, WCAG 2.1 SC 2.3.3 (DESIGN-04)"
    - "src/styles/globals.css imports tailwindcss + theme.css + fonts.css + animations.css in order; src/main.tsx imports globals.css as a side effect"
    - "Vitest test theme-tokens.test.ts asserts every documented v1.0 :root token has a corresponding @theme entry in theme.css AND parses the legacy :root + @media light + [data-theme=light] + [data-theme=dark] blocks to assert hex value parity for each renamed token (DESIGN-01 verification + checker warning 4 fix — no silent drift possible)"
    - "Vitest test animations.test.ts asserts all 8 keyframe names + the reduced-motion guard block exist in animations.css (DESIGN-03 verification)"
    - "D-08: all eight @keyframes (heroBlobPulse, holoOrbDrift, holoBtnSpin, holoIconSpin, holoUnderlineSlide, iridShift, bgPulse, silkShift) live in src/styles/animations.css as CSS keyframes — not Tailwind animation utilities — because they are multi-property and reused across many surfaces"
    - "D-09: component-level transitions (hover, focus, modal enter/exit) use Tailwind utilities with motion-safe: / motion-reduce: variants — not @keyframes — for Phase 1 placeholder + DesignSystem route surfaces"
    - "D-18: Tailwind v4 CSS-first @theme block in src/styles/theme.css is the source of truth for tokens; tailwind.config.ts (in plan 02) is a thin shim for content paths and plugins only — no v3-style JS-config token definitions"
    - "D-20: variable WOFF2 for DM Sans (300–700, opsz 9–40) and Playfair Display (400–900, italic) loaded via @fontsource-variable npm packages with font-display: swap — production build contains zero references to fonts.googleapis.com or fonts.gstatic.com (DESIGN-02)"
  artifacts:
    - path: "src/styles/theme.css"
      provides: "@theme block — all v1.0 :root design tokens with Tailwind v4 prefix conventions"
      contains: "@theme"
      min_lines: 80
    - path: "src/styles/animations.css"
      provides: "8 @keyframes ported verbatim from additions.css + global prefers-reduced-motion guard"
      contains: "@keyframes heroBlobPulse"
      min_lines: 80
    - path: "src/styles/fonts.css"
      provides: "(optional) @font-face fallbacks; Phase 1 may rely entirely on Fontsource imports — file present but minimal"
    - path: "src/styles/globals.css"
      provides: "Root stylesheet — imports tailwindcss + theme.css + fonts.css + animations.css"
      contains: "@import 'tailwindcss'"
    - path: "src/main.tsx"
      provides: "Side-effect imports for Fontsource packages + globals.css (already done in plan 02 — verify still correct after this plan)"
    - path: "src/styles/__tests__/theme-tokens.test.ts"
      provides: "Token parity grep test — DESIGN-01 verification"
    - path: "src/styles/__tests__/animations.test.ts"
      provides: "Keyframe presence + reduced-motion guard grep test — DESIGN-03/04 verification"
  key_links:
    - from: "src/styles/theme.css"
      to: "public/legacy/css/style.css :root block"
      via: "Token parity (renamed prefixes; identical values)"
      pattern: "@theme"
    - from: "src/styles/animations.css"
      to: "public/legacy/css/additions.css 8 @keyframes blocks"
      via: "Verbatim port"
      pattern: "@keyframes "
    - from: "src/styles/animations.css"
      to: "WCAG 2.1 SC 2.3.3 reduced-motion guard"
      via: "@media (prefers-reduced-motion: reduce) block disables all 8 animations + transitions"
      pattern: "prefers-reduced-motion: reduce"
    - from: "src/main.tsx"
      to: "@fontsource-variable/* packages"
      via: "Side-effect imports inject @font-face CSS at build time (DESIGN-02)"
      pattern: "@fontsource-variable"
---

<objective>
Reproduce the Celestial Map design system as a Tailwind v4 + CSS-first artifact set: design tokens (DESIGN-01), self-hosted variable fonts (DESIGN-02), 8 keyframe animations (DESIGN-03), and a comprehensive `prefers-reduced-motion: reduce` guard (DESIGN-04). All values are ported verbatim from the v1.0 CSS files (`public/legacy/css/style.css` + `public/legacy/css/additions.css`).

Tailwind v4 token naming gotcha (Pitfall 1): the `@theme` directive auto-generates utilities from prefixed token names (`--color-*`, `--font-*`, `--shadow-*`, `--radius-*`). v1.0 used unprefixed names (`--bg`, `--glow`). The port keeps VALUES identical but RENAMES the variables — `--bg` → `--color-bg`, `--glow` → `--shadow-glow`, etc. The `tests/theme-tokens.test.ts` enforces both directions: every v1.0 token has a v2.0 entry, every value is byte-identical.

D-08: 8 keyframes ported as global CSS, NOT Tailwind animation utilities (utility-soup would lose readability for multi-property animations).

D-10: prefers-reduced-motion DISABLES all animations entirely (not just slows them) — WCAG 2.1 SC 2.3.3 compliant. v1.0 only guards `heroBlobPulse` (CONCERNS.md gap); the port fixes this.

Output: 3 main stylesheets (theme.css, animations.css, fonts.css optional) + globals.css aggregator + 2 Vitest grep tests. Token parity test + keyframe-presence test both pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-skeleton/01-CONTEXT.md
@.planning/phases/01-skeleton/01-RESEARCH.md
@.planning/phases/01-skeleton/01-PATTERNS.md
@public/legacy/css/style.css
@public/legacy/css/additions.css
</context>

<tasks>

<task type="auto">
  <name>Task 7.1: Port theme.css (DESIGN-01) — Tailwind v4 @theme block with all v1.0 :root tokens + light/dark/auto variants + globals.css aggregator + theme-tokens parity test</name>
  <files>src/styles/theme.css, src/styles/globals.css, src/styles/__tests__/theme-tokens.test.ts</files>
  <read_first>
    - public/legacy/css/style.css (lines 1-100 — :root block + all token declarations + @media (prefers-color-scheme: light) override + [data-theme="light"] override)
    - .planning/phases/01-skeleton/01-RESEARCH.md (section "Pattern 2: Tailwind v4 @theme block for design tokens" — token name remapping table)
    - .planning/phases/01-skeleton/01-PATTERNS.md (section `src/styles/theme.css` — explicit token name remapping list, Pitfall 1)
    - .planning/phases/01-skeleton/01-CONTEXT.md (D-18, D-19)
  </read_first>
  <action>
    Read `public/legacy/css/style.css` lines 1-100. Identify every `:root` token. v1.0 uses these documented tokens (per RESEARCH.md + PATTERNS.md):

    Core palette: `--bg`, `--bg-2`, `--surface`, `--surface-2`, `--surface-3`, `--text`, `--muted`, `--line`
    Brand: `--primary`, `--primary-strong`, `--accent`
    Status: `--green`, `--red`
    Typography: `--font-sans`, `--font-heading`
    Elevation: `--shadow`, `--shadow-sm`
    Glass surfaces: `--glass`, `--glass-border`, `--glass-blur`
    Glow: `--glow`, `--glow-sm`
    Radii: `--radius`, `--radius-lg`

    The Tailwind v4 prefix remapping (Pitfall 1):
    - `--bg`, `--bg-2`, `--surface`, `--surface-2`, `--surface-3`, `--text`, `--muted`, `--line` → `--color-bg`, `--color-bg-2`, `--color-surface`, `--color-surface-2`, `--color-surface-3`, `--color-text`, `--color-muted`, `--color-line`
    - `--primary`, `--primary-strong`, `--accent`, `--green`, `--red` → `--color-primary`, `--color-primary-strong`, `--color-accent`, `--color-green`, `--color-red`
    - `--glass`, `--glass-border` → `--color-glass`, `--color-glass-border`
    - `--glass-blur` → keep unprefixed (it is a filter value, not a color)
    - `--glow`, `--glow-sm` → `--shadow-glow`, `--shadow-glow-sm` (Tailwind shadow utilities)
    - `--font-sans`, `--font-heading` → keep as-is
    - `--shadow`, `--shadow-sm` → keep as-is
    - `--radius`, `--radius-lg` → keep as-is

    Write `src/styles/theme.css`:

    ```
    /* src/styles/theme.css
     * Source: ported from public/legacy/css/style.css :root block.
     * DESIGN-01: every v1.0 token expressed as a Tailwind v4 @theme entry with renamed prefixes (Pitfall 1).
     * D-18: CSS-first @theme directive is the source of truth.
     * D-19: dark default; light override via [data-theme="light"]; auto via prefers-color-scheme.
     * Token values are BYTE-IDENTICAL to v1.0 — only variable names change.
     */
    @import 'tailwindcss';

    @theme {
      /* Core palette — dark default (matches v1.0 :root values) */
      --color-bg:         #07091a;
      --color-bg-2:       #0c1028;
      --color-surface:    #111528;
      --color-surface-2:  #171d38;
      --color-surface-3:  #1d2344;
      --color-text:       #ede8ff;
      --color-muted:      #8880b8;
      --color-line:       rgba(130, 100, 220, 0.22);

      /* Brand — violet + magenta */
      --color-primary:        #b96eff;
      --color-primary-strong: #cf8fff;
      --color-accent:         #ff4da6;

      /* Status */
      --color-green: #22d460;
      --color-red:   #ff4545;

      /* Typography — variable fonts loaded via @fontsource-variable/* imports in main.tsx (DESIGN-02) */
      --font-sans:    'DM Sans Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif;
      --font-heading: 'Playfair Display Variable', Georgia, 'Times New Roman', serif;

      /* Elevation */
      --shadow:    0 24px 64px -12px rgba(0,0,0,.85), 0 4px 16px rgba(0,0,0,.5);
      --shadow-sm: 0 4px 16px -4px rgba(0,0,0,.5);

      /* Glass surfaces */
      --color-glass:        rgba(10, 13, 35, 0.72);
      --color-glass-border: rgba(130, 100, 220, 0.2);
      --glass-blur:         blur(20px) saturate(1.5);

      /* Neon glow */
      --shadow-glow:    0 0 22px rgba(185, 110, 255, 0.3), 0 0 44px rgba(185, 110, 255, 0.1);
      --shadow-glow-sm: 0 0 12px rgba(185, 110, 255, 0.25);

      /* Radii */
      --radius:    18px;
      --radius-lg: 28px;
    }

    /* Light theme override — applied via [data-theme="light"] selector on <html> (D-19) */
    :root[data-theme='light'] {
      --color-bg:        #f2eeff;
      --color-bg-2:      #e8dfff;
      --color-surface:   #ffffff;
      --color-surface-2: #ede5ff;
      --color-surface-3: #dfd4ff;
      --color-text:      #1a0f3a;
      --color-muted:     #6a5a9a;
      --color-line:      rgba(100, 70, 180, 0.22);

      --color-primary:        #7c3aed;
      --color-primary-strong: #6d28d9;
      --color-accent:         #db2777;

      --shadow:    0 16px 48px -12px rgba(90,50,170,.32), 0 4px 12px rgba(90,50,170,.14);
      --shadow-sm: 0 4px 16px -4px rgba(90,50,170,.22);

      --color-glass:        rgba(255, 255, 255, 0.80);
      --color-glass-border: rgba(100, 70, 180, 0.25);

      --shadow-glow:    0 0 28px rgba(124, 58, 237, 0.28), 0 0 56px rgba(124, 58, 237, 0.12);
      --shadow-glow-sm: 0 0 16px rgba(124, 58, 237, 0.24);
    }

    /* Auto mode: follow OS prefers-color-scheme */
    @media (prefers-color-scheme: light) {
      :root[data-theme='auto'],
      :root:not([data-theme]) {
        --color-bg:        #f2eeff;
        --color-bg-2:      #e8dfff;
        --color-surface:   #ffffff;
        --color-surface-2: #ede5ff;
        --color-surface-3: #dfd4ff;
        --color-text:      #1a0f3a;
        --color-muted:     #6a5a9a;
        --color-line:      rgba(100, 70, 180, 0.22);

        --color-primary:        #7c3aed;
        --color-primary-strong: #6d28d9;
        --color-accent:         #db2777;

        --shadow:    0 16px 48px -12px rgba(90,50,170,.32), 0 4px 12px rgba(90,50,170,.14);
        --shadow-sm: 0 4px 16px -4px rgba(90,50,170,.22);

        --color-glass:        rgba(255, 255, 255, 0.80);
        --color-glass-border: rgba(100, 70, 180, 0.25);

        --shadow-glow:    0 0 28px rgba(124, 58, 237, 0.28), 0 0 56px rgba(124, 58, 237, 0.12);
        --shadow-glow-sm: 0 0 16px rgba(124, 58, 237, 0.24);
      }
    }

    /* Dark explicit — same as default but pinned regardless of OS preference */
    :root[data-theme='dark'] {
      --color-bg:         #07091a;
      --color-bg-2:       #0c1028;
      --color-surface:    #111528;
      --color-surface-2:  #171d38;
      --color-surface-3:  #1d2344;
      --color-text:       #ede8ff;
      --color-muted:      #8880b8;
      --color-line:       rgba(130, 100, 220, 0.22);

      --color-primary:        #b96eff;
      --color-primary-strong: #cf8fff;
      --color-accent:         #ff4da6;

      --shadow:    0 24px 64px -12px rgba(0,0,0,.85), 0 4px 16px rgba(0,0,0,.5);
      --shadow-sm: 0 4px 16px -4px rgba(0,0,0,.5);

      --color-glass:        rgba(10, 13, 35, 0.72);
      --color-glass-border: rgba(130, 100, 220, 0.2);

      --shadow-glow:    0 0 22px rgba(185, 110, 255, 0.3), 0 0 44px rgba(185, 110, 255, 0.1);
      --shadow-glow-sm: 0 0 12px rgba(185, 110, 255, 0.25);
    }
    ```

    CRITICAL: The HEX VALUES above are templated from RESEARCH.md Pattern 2 + PATTERNS.md. The executor MUST cross-check them against `public/legacy/css/style.css` lines 1-100 and REPLACE any mismatching value with the legacy file's actual value. The legacy CSS is the source of truth. The token parity test in this task will fail if values diverge.

    Write/update `src/styles/globals.css` (the shadcn-vite seed wrote it as `index.css` in plan 02; plan 02 already moved it to `src/styles/globals.css`):

    ```
    /* src/styles/globals.css
     * Root stylesheet aggregator. Imported as a side effect from src/main.tsx.
     * Order matters: tailwindcss must be imported first (via theme.css), then animations.
     */
    @import './theme.css';
    @import './fonts.css';
    @import './animations.css';
    ```

    If the seed left an `@import "tailwindcss"` directly inside globals.css OR an explicit `@layer` block, REMOVE it — theme.css already imports tailwindcss as line 1. Avoid double-imports.

    Write `src/styles/__tests__/theme-tokens.test.ts`:

    ```
    // src/styles/__tests__/theme-tokens.test.ts
    // DESIGN-01: every v1.0 :root token has a corresponding @theme entry in theme.css with the same hex value
    // (modulo the prefix rename). Now also asserts light-mode hex value parity per checker warning 4 —
    // silent drift from v1.0 light-mode colors is no longer possible.
    // Vitest env: node (default per D-25).
    import { describe, it, expect } from 'vitest'
    import { readFileSync } from 'node:fs'
    import { resolve } from 'node:path'

    // Map from v1.0 unprefixed token name → v2.0 prefixed token name
    const TOKEN_RENAME: Record<string, string> = {
      '--bg':              '--color-bg',
      '--bg-2':            '--color-bg-2',
      '--surface':         '--color-surface',
      '--surface-2':       '--color-surface-2',
      '--surface-3':       '--color-surface-3',
      '--text':            '--color-text',
      '--muted':           '--color-muted',
      '--line':            '--color-line',
      '--primary':         '--color-primary',
      '--primary-strong':  '--color-primary-strong',
      '--accent':          '--color-accent',
      '--green':           '--color-green',
      '--red':             '--color-red',
      '--glass':           '--color-glass',
      '--glass-border':    '--color-glass-border',
      '--glass-blur':      '--glass-blur',
      '--glow':            '--shadow-glow',
      '--glow-sm':         '--shadow-glow-sm',
      '--font-sans':       '--font-sans',
      '--font-heading':    '--font-heading',
      '--shadow':          '--shadow',
      '--shadow-sm':       '--shadow-sm',
      '--radius':          '--radius',
      '--radius-lg':       '--radius-lg',
    }

    function readThemeCss(): string {
      return readFileSync(resolve(__dirname, '../theme.css'), 'utf-8')
    }

    function readLegacyStyleCss(): string {
      return readFileSync(resolve(__dirname, '../../../public/legacy/css/style.css'), 'utf-8')
    }

    /**
     * Parse a CSS block bounded by a header regex and the next \}. Returns a Map of
     * variable-name → value. Whitespace is preserved in values to match exact byte parity.
     * Robust enough for the two blocks we care about (the legacy :root dark block + the
     * @media (prefers-color-scheme: light) inner block + the [data-theme="light"] block).
     */
    function parseTokens(css: string, blockHeaderRe: RegExp): Map<string, string> {
      const headMatch = blockHeaderRe.exec(css)
      if (!headMatch) return new Map()
      const start = headMatch.index + headMatch[0].length
      // Find the matching closing brace (naive — handles single nesting fine for these blocks)
      let depth = 1
      let i = start
      while (i < css.length && depth > 0) {
        const ch = css[i]
        if (ch === '{') depth++
        else if (ch === '}') depth--
        if (depth === 0) break
        i++
      }
      const body = css.slice(start, i)
      const out = new Map<string, string>()
      // Match "  --name: value;" lines. The value can be anything up to the semicolon.
      const lineRe = /(--[a-z0-9-]+)\s*:\s*([^;]+);/gi
      let m: RegExpExecArray | null
      while ((m = lineRe.exec(body)) !== null) {
        const name = m[1]
        const value = m[2].trim().replace(/\s+/g, ' ')
        if (name) out.set(name, value)
      }
      return out
    }

    function normalise(value: string): string {
      // Normalise whitespace to be tolerant of formatting differences; values are still compared exactly otherwise.
      return value.replace(/\s+/g, ' ').trim()
    }

    describe('design tokens — v1.0 → v2.0 parity (DESIGN-01, Pitfall 1)', () => {
      it('theme.css imports tailwindcss', () => {
        const css = readThemeCss()
        expect(css).toMatch(/@import\s+['"]tailwindcss['"]/)
      })

      it('theme.css declares a @theme block', () => {
        const css = readThemeCss()
        expect(css).toMatch(/@theme\s*\{/)
      })

      it('every v1.0 :root token has a corresponding v2.0 @theme entry (renamed per Pitfall 1)', () => {
        const css = readThemeCss()
        for (const [v1Name, v2Name] of Object.entries(TOKEN_RENAME)) {
          // The escape is for the leading '--' (regex-safe)
          const re = new RegExp(`\\${v2Name}\\s*:`)
          expect(css, `Missing v2 token ${v2Name} (was v1 ${v1Name})`).toMatch(re)
        }
      })

      it('theme.css declares both [data-theme="light"] and [data-theme="dark"] overrides', () => {
        const css = readThemeCss()
        expect(css).toMatch(/\[data-theme=['"]light['"]\]/)
        expect(css).toMatch(/\[data-theme=['"]dark['"]\]/)
      })

      it('theme.css declares an auto-mode @media (prefers-color-scheme: light) block', () => {
        const css = readThemeCss()
        expect(css).toMatch(/@media\s*\(prefers-color-scheme:\s*light\)/)
      })

      it('contains no fonts.googleapis.com or fonts.gstatic.com references (DESIGN-02 sanity)', () => {
        const css = readThemeCss()
        expect(css).not.toMatch(/fonts\.googleapis\.com/)
        expect(css).not.toMatch(/fonts\.gstatic\.com/)
      })

      it('legacy style.css :root tokens still exist (sanity — plan 01 moved file did not corrupt it)', () => {
        const legacy = readLegacyStyleCss()
        expect(legacy).toMatch(/--bg:\s*#07091a/)
        expect(legacy).toMatch(/--primary:\s*#b96eff/)
      })

      // ------------------------------------------------------------------
      // Checker warning 4: assert light-mode hex value parity, not just token names.
      // Parse v1.0 :root + @media (prefers-color-scheme: light) + [data-theme="light"]
      // blocks, then assert every renamed token has the same value in theme.css.
      // ------------------------------------------------------------------
      it('dark-mode hex values from legacy :root block match theme.css renamed counterparts', () => {
        const legacy = readLegacyStyleCss()
        const theme = readThemeCss()
        // Legacy dark :root block — matches the combined selector ":root, :root[data-theme=\"dark\"], :root[data-theme=\"auto\"] {"
        const legacyDark = parseTokens(legacy, /:root\s*,\s*:root\[data-theme="dark"\]\s*,\s*:root\[data-theme="auto"\]\s*\{/)
        const themeDefault = parseTokens(theme, /@theme\s*\{/)
        for (const [v1Name, v2Name] of Object.entries(TOKEN_RENAME)) {
          const v1Value = legacyDark.get(v1Name)
          if (v1Value === undefined) continue // some tokens (e.g. --font-sans family list) may legitimately differ when the v2.0 name is the same — skip if absent
          const v2Value = themeDefault.get(v2Name)
          expect(v2Value, `dark-mode ${v2Name} missing from theme.css @theme block (v1.0 was ${v1Name}: ${v1Value})`).toBeTruthy()
          // For color tokens, compare normalised values byte-for-byte. For typography (font family lists),
          // skip the byte compare because Fontsource variant names legitimately rename the family
          // (e.g. "DM Sans" → "DM Sans Variable").
          if (!v2Name.startsWith('--font-')) {
            expect(normalise(v2Value as string), `dark-mode ${v2Name} value drift from v1.0 (expected ${v1Value}, got ${v2Value})`).toBe(normalise(v1Value))
          }
        }
      })

      it('light-mode hex values from legacy @media (prefers-color-scheme: light) block match theme.css @media block', () => {
        const legacy = readLegacyStyleCss()
        const theme = readThemeCss()
        // Legacy light block via @media — the inner selector is ":root[data-theme=\"auto\"], :root:not([data-theme]) {"
        const legacyLight = parseTokens(legacy, /:root\[data-theme="auto"\]\s*,\s*:root:not\(\[data-theme\]\)\s*\{/)
        // theme.css @media block — match the inner :root[data-theme='auto'] / :root:not([data-theme]) selector
        const themeAutoLight = parseTokens(theme, /:root\[data-theme=['"]auto['"]\]\s*,\s*:root:not\(\[data-theme\]\)\s*\{/)
        expect(legacyLight.size, 'failed to parse legacy light-mode block').toBeGreaterThan(0)
        expect(themeAutoLight.size, 'failed to parse theme.css auto-light block').toBeGreaterThan(0)
        for (const [v1Name, v2Name] of Object.entries(TOKEN_RENAME)) {
          const v1Value = legacyLight.get(v1Name)
          if (v1Value === undefined) continue // not every token is overridden in light mode (e.g. --glass-blur stays the same)
          if (v2Name.startsWith('--font-')) continue
          const v2Value = themeAutoLight.get(v2Name)
          expect(v2Value, `light-mode (auto) ${v2Name} missing from theme.css (v1.0 was ${v1Name}: ${v1Value})`).toBeTruthy()
          expect(normalise(v2Value as string), `light-mode (auto) ${v2Name} value drift from v1.0 (expected ${v1Value}, got ${v2Value})`).toBe(normalise(v1Value))
        }
      })

      it('light-mode hex values from legacy [data-theme="light"] block match theme.css [data-theme="light"] block', () => {
        const legacy = readLegacyStyleCss()
        const theme = readThemeCss()
        const legacyLightExplicit = parseTokens(legacy, /:root\[data-theme="light"\]\s*\{/)
        const themeLightExplicit = parseTokens(theme, /:root\[data-theme=['"]light['"]\]\s*\{/)
        expect(legacyLightExplicit.size, 'failed to parse legacy [data-theme=light] block').toBeGreaterThan(0)
        expect(themeLightExplicit.size, 'failed to parse theme.css [data-theme=light] block').toBeGreaterThan(0)
        for (const [v1Name, v2Name] of Object.entries(TOKEN_RENAME)) {
          const v1Value = legacyLightExplicit.get(v1Name)
          if (v1Value === undefined) continue
          if (v2Name.startsWith('--font-')) continue
          const v2Value = themeLightExplicit.get(v2Name)
          expect(v2Value, `[data-theme=light] ${v2Name} missing (v1.0 was ${v1Name}: ${v1Value})`).toBeTruthy()
          expect(normalise(v2Value as string), `[data-theme=light] ${v2Name} value drift from v1.0 (expected ${v1Value}, got ${v2Value})`).toBe(normalise(v1Value))
        }
      })

      it('dark-mode explicit [data-theme="dark"] block matches legacy dark values', () => {
        const legacy = readLegacyStyleCss()
        const theme = readThemeCss()
        const legacyDark = parseTokens(legacy, /:root\s*,\s*:root\[data-theme="dark"\]\s*,\s*:root\[data-theme="auto"\]\s*\{/)
        const themeDarkExplicit = parseTokens(theme, /:root\[data-theme=['"]dark['"]\]\s*\{/)
        expect(themeDarkExplicit.size, 'failed to parse theme.css [data-theme=dark] block').toBeGreaterThan(0)
        for (const [v1Name, v2Name] of Object.entries(TOKEN_RENAME)) {
          const v1Value = legacyDark.get(v1Name)
          if (v1Value === undefined) continue
          if (v2Name.startsWith('--font-')) continue
          const v2Value = themeDarkExplicit.get(v2Name)
          expect(v2Value, `[data-theme=dark] ${v2Name} missing (v1.0 was ${v1Name}: ${v1Value})`).toBeTruthy()
          expect(normalise(v2Value as string), `[data-theme=dark] ${v2Name} value drift from v1.0 (expected ${v1Value}, got ${v2Value})`).toBe(normalise(v1Value))
        }
      })
    })
    ```

    Run:

    ```
    pnpm run typecheck
    pnpm run test -- --run src/styles/__tests__/theme-tokens.test.ts
    ```

    Critical do-NOTs:
    - Do NOT use `dark:` Tailwind variants — D-19 mandates `data-theme` attribute selectors (the source of truth is the CSS variables; Tailwind utilities consume them)
    - Do NOT add tokens that don't exist in v1.0 — content-frozen on the design side too
    - Do NOT use v1.0's old unprefixed names like `--bg` — Tailwind v4 needs `--color-bg` to auto-generate utilities (Pitfall 1)
    - Do NOT @import any Google Fonts URL in theme.css
  </action>
  <verify>
    <automated>test -f src/styles/theme.css && test -f src/styles/globals.css && test -f src/styles/__tests__/theme-tokens.test.ts && grep -c "@import 'tailwindcss'" src/styles/theme.css && grep -c "@theme" src/styles/theme.css && grep -c -- "--color-bg:" src/styles/theme.css && grep -c -- "--color-primary:" src/styles/theme.css && grep -c -- "--color-accent:" src/styles/theme.css && grep -c -- "--shadow-glow:" src/styles/theme.css && grep -c "data-theme='light'\|data-theme=\"light\"" src/styles/theme.css && grep -c "data-theme='dark'\|data-theme=\"dark\"" src/styles/theme.css && grep -c "prefers-color-scheme: light" src/styles/theme.css && grep -c -- "fonts.googleapis.com\|fonts.gstatic.com" src/styles/theme.css; pnpm run typecheck && pnpm run test -- --run src/styles/__tests__/theme-tokens.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/styles/theme.css` returns 0
    - `test -f src/styles/globals.css` returns 0
    - `test -f src/styles/__tests__/theme-tokens.test.ts` returns 0
    - `grep -c "@import 'tailwindcss'\|@import \"tailwindcss\"" src/styles/theme.css` returns at least 1
    - `grep -c "@theme" src/styles/theme.css` returns at least 1
    - `grep -c -- "--color-bg:" src/styles/theme.css` returns at least 2 (default + light override)
    - `grep -c -- "--color-primary:" src/styles/theme.css` returns at least 2
    - `grep -c -- "--color-accent:" src/styles/theme.css` returns at least 2
    - `grep -c -- "--color-text:" src/styles/theme.css` returns at least 2
    - `grep -c -- "--shadow-glow:" src/styles/theme.css` returns at least 2
    - `grep -c -- "--font-sans:" src/styles/theme.css` returns at least 1
    - `grep -c -- "--font-heading:" src/styles/theme.css` returns at least 1
    - `grep -c -- "--radius:" src/styles/theme.css` returns at least 1
    - `grep -c -- "--glass-blur:" src/styles/theme.css` returns at least 1
    - `grep -c "data-theme='light'\|data-theme=\"light\"" src/styles/theme.css` returns at least 1
    - `grep -c "data-theme='dark'\|data-theme=\"dark\"" src/styles/theme.css` returns at least 1
    - `grep -c "prefers-color-scheme: light" src/styles/theme.css` returns at least 1
    - `grep -c "fonts.googleapis.com\|fonts.gstatic.com" src/styles/theme.css` returns 0
    - `pnpm run typecheck` exits 0
    - `pnpm run test -- --run src/styles/__tests__/theme-tokens.test.ts` exits 0 with all 11 assertions passing (7 original + 4 light/dark-mode hex value parity assertions per checker warning 4)
  </acceptance_criteria>
  <done>
    theme.css declares the v1.0 Celestial Map palette as a Tailwind v4 @theme block with renamed prefixes (Pitfall 1). Light + dark + auto variants resolve via `[data-theme]` selectors and `prefers-color-scheme` media query (D-19). All 24 documented v1.0 tokens have a v2.0 counterpart per the TOKEN_RENAME map. globals.css aggregates theme.css + fonts.css + animations.css. The token parity Vitest test passes 11 assertions: 7 structural (presence + selectors + import + Google-Fonts sanity) PLUS 4 hex-value parity assertions that parse the legacy :root, @media (prefers-color-scheme: light), [data-theme=light], and [data-theme=dark] blocks and assert each renamed token's value is byte-identical in theme.css. Silent drift from v1.0 light-mode colors is now impossible.
  </done>
</task>

<task type="auto">
  <name>Task 7.2: Port animations.css (DESIGN-03) — 8 @keyframes verbatim + global prefers-reduced-motion guard (DESIGN-04, D-10, WCAG 2.1 SC 2.3.3) + write fonts.css placeholder + animations parity test</name>
  <files>src/styles/animations.css, src/styles/fonts.css, src/styles/__tests__/animations.test.ts</files>
  <read_first>
    - public/legacy/css/additions.css (lines 825-1410 — search for all 8 @keyframes: heroBlobPulse, holoOrbDrift, holoBtnSpin, holoIconSpin, holoUnderlineSlide, iridShift, bgPulse, silkShift)
    - .planning/phases/01-skeleton/01-PATTERNS.md (section `src/styles/animations.css` — keyframe sources + reduced-motion guard structure)
    - .planning/phases/01-skeleton/01-CONTEXT.md (D-08, D-10)
    - .planning/phases/01-skeleton/01-RESEARCH.md (Pitfall 6 — duration zeroing is insufficient; need explicit animation: none on the targets)
  </read_first>
  <action>
    Open `public/legacy/css/additions.css` and locate the 8 keyframe blocks. They appear at approximately lines 828, 833, 873, 903, 933, 964, 1367, 1398 per RESEARCH.md. Use `grep -n '@keyframes' public/legacy/css/additions.css` to find exact line numbers.

    Read each keyframe block in full (typically 5-15 lines each). PORT THEM VERBATIM into `src/styles/animations.css`. Preserve the keyframe names exactly (kebab-case identifiers are not legal CSS — keep camelCase as v1.0 wrote them).

    Also identify which selectors in v1.0 USE each keyframe (e.g. `body::before { animation: heroBlobPulse 6s ease-in-out infinite; }`). The selectors live inside `additions.css` outside the keyframe blocks. For Phase 1, we don't need to replicate every usage — only ensure the keyframes are defined so they can be referenced when Phase 2 ports the views. BUT we DO need to replicate enough selector → animation bindings to make the reduced-motion override target them (otherwise `animation: none` on a non-existent selector is a no-op).

    Use this template, replacing the keyframe BODIES with the exact text from `public/legacy/css/additions.css`:

    ```
    /* src/styles/animations.css
     * Source: ported verbatim from public/legacy/css/additions.css @keyframes blocks (8 total).
     * DESIGN-03: keyframe names preserved (heroBlobPulse, holoOrbDrift, holoBtnSpin, holoIconSpin,
     *            holoUnderlineSlide, iridShift, bgPulse, silkShift).
     * DESIGN-04 + D-10 + WCAG 2.1 SC 2.3.3: prefers-reduced-motion guard DISABLES all 8 animations
     *            (Pitfall 6 — duration zeroing alone is insufficient; explicit animation: none required).
     */

    /* 1. Hero blob pulse */
    @keyframes heroBlobPulse {
      /* <PASTE FROM public/legacy/css/additions.css ~line 828> */
    }

    /* 2. Holo orb drift */
    @keyframes holoOrbDrift {
      /* <PASTE FROM ~line 833> */
    }

    /* 3. Holo button spin */
    @keyframes holoBtnSpin {
      /* <PASTE FROM ~line 873> */
    }

    /* 4. Holo icon spin */
    @keyframes holoIconSpin {
      /* <PASTE FROM ~line 903> */
    }

    /* 5. Holo underline slide */
    @keyframes holoUnderlineSlide {
      /* <PASTE FROM ~line 933> */
    }

    /* 6. Iridescent shift */
    @keyframes iridShift {
      /* <PASTE FROM ~line 964> */
    }

    /* 7. Background pulse */
    @keyframes bgPulse {
      /* <PASTE FROM ~line 1367> */
    }

    /* 8. Silk shift */
    @keyframes silkShift {
      /* <PASTE FROM ~line 1398> */
    }

    /* ------------------------------------------------------------------
     * Reduced-motion guard — DESIGN-04, D-10, WCAG 2.1 SC 2.3.3.
     * Fixes v1.0's gap: v1.0 only guarded heroBlobPulse; v2.0 disables all 8.
     * Pitfall 6: duration zeroing alone is not enough for infinite animations.
     * ------------------------------------------------------------------ */
    @media (prefers-reduced-motion: reduce) {
      *, *::before, *::after {
        animation-duration: 0.001ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.001ms !important;
        transition-delay: 0ms !important;
      }
      /* Explicit per-keyframe disable (Pitfall 6) — selectors are the standard targets the keyframes are applied to in v1.0.
       * If a Phase 2 component uses a different selector for the same keyframe, the universal rule above still applies. */
      .hero-blob,
      .hero-blob-holo,
      body::before,
      body::after,
      .btn-primary::after,
      .btn-primary::before,
      .hero-feat-icon::before,
      .section-head h2::after,
      .page-head h1::after,
      .scale-btn.is-active::after,
      .scale-pill.is-active::after,
      .li-avatar::before,
      .card::after,
      .card-add,
      .rs-modal-card::before {
        animation: none !important;
      }
    }

    /* In-page reduced-motion preview toggle (CONTEXT.md <specifics>): mirrors the @media block for the
     * /design-system page so reviewers can eyeball reduced-motion without changing OS settings (plan 08). */
    body[data-prm='reduce'] *,
    body[data-prm='reduce'] *::before,
    body[data-prm='reduce'] *::after {
      animation-duration: 0.001ms !important;
      animation-iteration-count: 1 !important;
      transition-duration: 0.001ms !important;
      transition-delay: 0ms !important;
    }
    body[data-prm='reduce'] .hero-blob,
    body[data-prm='reduce'] .hero-blob-holo,
    body[data-prm='reduce'] body::before,
    body[data-prm='reduce'] body::after,
    body[data-prm='reduce'] .btn-primary::after,
    body[data-prm='reduce'] .btn-primary::before,
    body[data-prm='reduce'] .hero-feat-icon::before,
    body[data-prm='reduce'] .section-head h2::after,
    body[data-prm='reduce'] .page-head h1::after,
    body[data-prm='reduce'] .scale-btn.is-active::after,
    body[data-prm='reduce'] .scale-pill.is-active::after,
    body[data-prm='reduce'] .li-avatar::before,
    body[data-prm='reduce'] .card::after,
    body[data-prm='reduce'] .card-add,
    body[data-prm='reduce'] .rs-modal-card::before {
      animation: none !important;
    }
    ```

    The selector list for `animation: none !important` is best-effort based on PATTERNS.md note "selectors are the standard targets the keyframes are applied to in v1.0". To make it tighter, grep `public/legacy/css/additions.css` for `animation: ` references and ensure every infinite animation has a corresponding disable selector. If you discover a selector in v1.0 that uses one of the 8 keyframes but is not in the list, ADD it.

    Write `src/styles/fonts.css` as a near-empty file. The Fontsource imports in `src/main.tsx` inject `@font-face` declarations automatically; this file exists for the import chain in `globals.css` and as a placeholder if hand-rolled font fallbacks become necessary:

    ```
    /* src/styles/fonts.css
     * Self-hosted variable fonts are loaded via @fontsource-variable/* side-effect imports in src/main.tsx.
     * Those packages inject @font-face declarations with font-display: swap automatically (DESIGN-02).
     * This file exists for the @import chain in globals.css and as a place for hand-rolled fallbacks
     * if RESEARCH.md assumption A2 ever needs to invert.
     */
    ```

    Re-verify `src/main.tsx` (plan 02 should have set this up already; this task validates):

    ```
    import '@fontsource-variable/dm-sans'
    import '@fontsource-variable/playfair-display'
    import './styles/globals.css'
    ```

    If those three lines are NOT in `src/main.tsx`, ADD them. Order matters: fonts before globals.css so the @font-face declarations are registered before any consumer.

    Write `src/styles/__tests__/animations.test.ts`:

    ```
    // src/styles/__tests__/animations.test.ts
    // DESIGN-03 + DESIGN-04: 8 keyframe names present + reduced-motion guard exists.
    import { describe, it, expect } from 'vitest'
    import { readFileSync } from 'node:fs'
    import { resolve } from 'node:path'

    const EXPECTED_KEYFRAMES = [
      'heroBlobPulse',
      'holoOrbDrift',
      'holoBtnSpin',
      'holoIconSpin',
      'holoUnderlineSlide',
      'iridShift',
      'bgPulse',
      'silkShift',
    ]

    function readAnimationsCss(): string {
      return readFileSync(resolve(__dirname, '../animations.css'), 'utf-8')
    }

    describe('animations.css (DESIGN-03, DESIGN-04)', () => {
      it('declares all 8 v1.0 @keyframes', () => {
        const css = readAnimationsCss()
        for (const name of EXPECTED_KEYFRAMES) {
          const re = new RegExp(`@keyframes\\s+${name}\\b`)
          expect(css, `Missing @keyframes ${name}`).toMatch(re)
        }
      })

      it('contains a @media (prefers-reduced-motion: reduce) block', () => {
        const css = readAnimationsCss()
        expect(css).toMatch(/@media\s*\(prefers-reduced-motion:\s*reduce\)/)
      })

      it('reduced-motion block sets animation-duration: 0.001ms universally', () => {
        const css = readAnimationsCss()
        // Strip line-comments for safe `grep`-style assertion (Pitfall — comments could match the pattern themselves)
        const withoutComments = css.replace(/\/\*[\s\S]*?\*\//g, '')
        expect(withoutComments).toMatch(/animation-duration:\s*0\.001ms\s*!important/)
        expect(withoutComments).toMatch(/animation-iteration-count:\s*1\s*!important/)
        expect(withoutComments).toMatch(/transition-duration:\s*0\.001ms\s*!important/)
      })

      it('reduced-motion block explicitly sets animation: none !important on at least 5 keyframe targets (Pitfall 6)', () => {
        const css = readAnimationsCss()
        const withoutComments = css.replace(/\/\*[\s\S]*?\*\//g, '')
        const noneCount = (withoutComments.match(/animation:\s*none\s*!important/g) ?? []).length
        expect(noneCount).toBeGreaterThanOrEqual(2) // at least once for @media + once for body[data-prm]
      })

      it('declares the in-page reduced-motion preview toggle (body[data-prm="reduce"] selector for /design-system page)', () => {
        const css = readAnimationsCss()
        expect(css).toMatch(/body\[data-prm=['"]reduce['"]\]/)
      })
    })
    ```

    Run:

    ```
    pnpm run typecheck
    pnpm run lint
    pnpm run test -- --run src/styles/__tests__/animations.test.ts
    ```

    Critical do-NOTs:
    - Do NOT convert keyframes to Tailwind `animation-*` utilities — D-08 explicitly mandates global CSS
    - Do NOT rename keyframes — v1.0 + v2.0 must agree on identifier (future Phase 2 ports of legacy view code will reference the same names)
    - Do NOT skip any of the 8 keyframes — DESIGN-03 mandates all 8
    - Do NOT rely solely on duration zeroing — Pitfall 6 requires explicit `animation: none` on the keyframe targets
  </action>
  <verify>
    <automated>test -f src/styles/animations.css && test -f src/styles/fonts.css && test -f src/styles/__tests__/animations.test.ts && grep -c "@keyframes heroBlobPulse" src/styles/animations.css && grep -c "@keyframes holoOrbDrift" src/styles/animations.css && grep -c "@keyframes holoBtnSpin" src/styles/animations.css && grep -c "@keyframes holoIconSpin" src/styles/animations.css && grep -c "@keyframes holoUnderlineSlide" src/styles/animations.css && grep -c "@keyframes iridShift" src/styles/animations.css && grep -c "@keyframes bgPulse" src/styles/animations.css && grep -c "@keyframes silkShift" src/styles/animations.css && grep -c "prefers-reduced-motion: reduce" src/styles/animations.css && grep -c "animation: none !important" src/styles/animations.css && grep -c "data-prm='reduce'\|data-prm=\"reduce\"" src/styles/animations.css && grep -c "@fontsource-variable/dm-sans" src/main.tsx && grep -c "@fontsource-variable/playfair-display" src/main.tsx && pnpm run typecheck && pnpm run lint && pnpm run test -- --run src/styles/__tests__/animations.test.ts && pnpm run build && ! grep -r 'fonts.googleapis.com\|fonts.gstatic.com' dist/ 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/styles/animations.css` returns 0
    - `test -f src/styles/fonts.css` returns 0
    - `test -f src/styles/__tests__/animations.test.ts` returns 0
    - `grep -c "@keyframes heroBlobPulse" src/styles/animations.css` returns 1
    - `grep -c "@keyframes holoOrbDrift" src/styles/animations.css` returns 1
    - `grep -c "@keyframes holoBtnSpin" src/styles/animations.css` returns 1
    - `grep -c "@keyframes holoIconSpin" src/styles/animations.css` returns 1
    - `grep -c "@keyframes holoUnderlineSlide" src/styles/animations.css` returns 1
    - `grep -c "@keyframes iridShift" src/styles/animations.css` returns 1
    - `grep -c "@keyframes bgPulse" src/styles/animations.css` returns 1
    - `grep -c "@keyframes silkShift" src/styles/animations.css` returns 1
    - `grep -c "prefers-reduced-motion: reduce" src/styles/animations.css` returns at least 1
    - `grep -c "animation: none !important" src/styles/animations.css` returns at least 2 (covers @media block AND body[data-prm])
    - `grep -c "data-prm='reduce'\|data-prm=\"reduce\"" src/styles/animations.css` returns at least 1
    - `grep -c "@fontsource-variable/dm-sans" src/main.tsx` returns 1
    - `grep -c "@fontsource-variable/playfair-display" src/main.tsx` returns 1
    - `pnpm run typecheck` exits 0
    - `pnpm run lint` exits 0
    - `pnpm run test -- --run src/styles/__tests__/animations.test.ts` exits 0 with all 5 assertions passing
    - `pnpm run build` exits 0
    - `! grep -r 'fonts.googleapis.com' dist/` matches nothing (final-build DESIGN-02 check)
    - `! grep -r 'fonts.gstatic.com' dist/` matches nothing
  </acceptance_criteria>
  <done>
    animations.css contains all 8 v1.0 keyframes verbatim, a `@media (prefers-reduced-motion: reduce)` block with universal duration zeroing PLUS explicit `animation: none !important` on the v1.0 target selectors (Pitfall 6), and the in-page preview toggle (`body[data-prm='reduce']`) for the upcoming DesignSystem route. fonts.css is a near-empty placeholder. main.tsx imports the Fontsource variable packages (DESIGN-02). The Vitest animations test passes 5 assertions. The build output contains zero Google Fonts references.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fontsource npm packages → bundled WOFF2 | Variable WOFF2 files copied into dist/assets/; HTTP-fetched once on first load, then SW-cached |
| Tailwind v4 build output → produced utilities | Tailwind's CSS-first compiler reads @theme tokens and auto-generates `bg-bg`, `text-primary`, etc. |
| Reduced-motion media query → CSS engine | Browser-evaluated; DevTools "Emulate reduce" flag for testing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | DESIGN-02 violation — Google Fonts CDN still referenced in bundle | mitigate | Acceptance test 7.2 runs `grep -r fonts.googleapis.com dist/` and asserts zero matches. Plan 09 re-verifies as the phase-final gate |
| T-07-02 | Denial of Service | 8 simultaneous infinite animations on low-end Android cause GPU compositing jank | mitigate | DESIGN-04 reduced-motion guard disables all 8 entirely under `prefers-reduced-motion: reduce`; users with motion sensitivity or battery-saver get a calm UI |
| T-07-03 | Tampering | Token value drifts during port (e.g. --color-bg changes from #07091a to #08091a — single character typo) — every consumer renders differently | mitigate | theme-tokens parity test reads BOTH legacy style.css AND the new theme.css; future Phase 2 visual review against /legacy/ catches any drift |
| T-07-04 | Spoofing | A future reduced-motion gap (Phase 2 introduces a new animation without guarding it) silently violates WCAG | accept | Phase 1 establishes the universal `animation-duration: 0.001ms` baseline + explicit `animation: none` for the 8 known keyframes; Phase 2 review process owns extending the per-keyframe list |
| T-07-05 | Elevation of Privilege | A malicious @import in theme.css fetches arbitrary CSS from the network | accept | theme.css is committed source; not user input. The only @import is `tailwindcss` (Tailwind v4 directive) which the bundler resolves locally — no network at runtime |
</threat_model>

<verification>
After both tasks complete:

```
# All artifact files present
ls -la src/styles/

# Token parity test
pnpm run test -- --run src/styles/__tests__/theme-tokens.test.ts

# Animation + reduced-motion test
pnpm run test -- --run src/styles/__tests__/animations.test.ts

# Full build + zero Google Fonts refs
pnpm run build
! grep -r 'fonts.googleapis.com\|fonts.gstatic.com' dist/
```
</verification>

<success_criteria>
- theme.css declares the @theme block with every v1.0 :root token (renamed per Pitfall 1) and dark/light/auto variants
- animations.css contains all 8 v1.0 keyframes verbatim + a comprehensive reduced-motion guard with explicit `animation: none` on the 8 target selectors + the body[data-prm='reduce'] in-page preview hook
- fonts.css exists as a placeholder (the actual @font-face injection happens via Fontsource imports in main.tsx)
- globals.css imports theme.css + fonts.css + animations.css (Tailwind v4 import is via theme.css line 1)
- main.tsx side-effect-imports the Fontsource variable packages
- theme-tokens parity test passes (7 assertions, including DESIGN-02 sanity)
- animations test passes (5 assertions)
- pnpm run build produces a bundle with ZERO `fonts.googleapis.com` / `fonts.gstatic.com` references
- Requirements DESIGN-01, DESIGN-02, DESIGN-03, DESIGN-04 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-skeleton/01-07-SUMMARY.md` documenting:
- `wc -l` for theme.css and animations.css
- Confirmation that all 8 keyframe names grep-match
- The exact `grep -r fonts.googleapis.com dist/` output (must be empty)
- The exact `grep -r fonts.gstatic.com dist/` output (must be empty)
- A sample utility class invocation in src/routes/Placeholder.tsx (from plan 02) that uses one of the new tokens — confirms the @theme block is wired to Tailwind utility generation
- Pointer to plan 08 (Wave 6: app shell + design-system route)
</output>
