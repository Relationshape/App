---
phase: 01-skeleton
plan: 08
type: execute
wave: 5
depends_on:
  - "01-04"
  - "01-05"
  - "01-06"
  - "01-07"
files_modified:
  - src/router.tsx
  - src/App.tsx
  - src/routes/Placeholder.tsx
  - src/routes/DesignSystem.tsx
  - src/hooks/useTheme.ts
  - src/hooks/useLang.ts
  - src/components/ThemeToggle.tsx
  - src/components/LangToggle.tsx
  - src/__tests__/App.smoke.test.tsx
  - src/__tests__/DesignSystem.test.tsx
autonomous: true
requirements:
  - FOUND-06
  - DESIGN-05
  - DESIGN-06
tags:
  - router
  - hooks
  - theme
  - lang
  - design-system
  - smoke-test
  - react

must_haves:
  truths:
    - "src/router.tsx exports createHashRouter (D-02) with exactly two routes per D-03: / and /design-system"
    - "src/App.tsx renders RouterProvider and invokes useTheme to keep html data-theme in sync with the Zustand store"
    - "src/hooks/useTheme.ts reads settings.theme from useStore, applies data-theme to documentElement, listens to prefers-color-scheme when theme is auto (D-19)"
    - "src/hooks/useLang.ts reads settings.lang from useStore and exposes setLang delegating to the store action (D-14)"
    - "src/components/ThemeToggle.tsx renders three shadcn Buttons wired to setTheme (D-28); active button uses aria-pressed=true"
    - "src/components/LangToggle.tsx renders a select with EN+DE options wired to setLang"
    - "src/routes/DesignSystem.tsx renders the D-27 five sections plus the CONTEXT.md specifics reduced-motion preview toggle that adds data-prm='reduce' to body"
    - "src/routes/Placeholder.tsx renders 'Skeleton alive' content using at least one Tailwind utility derived from a v2.0 token (FOUND-02)"
    - "src/__tests__/App.smoke.test.tsx renders App in jsdom without crashing (FOUND-06)"
    - "src/__tests__/DesignSystem.test.tsx renders DesignSystem, clicks the dark theme toggle, asserts document.documentElement.dataset.theme === 'dark' (DESIGN-05)"
  artifacts:
    - path: "src/router.tsx"
      provides: "Hash router with / and /design-system routes (D-02, D-03)"
      contains: "createHashRouter"
    - path: "src/App.tsx"
      provides: "Top-level component invoking useTheme and rendering RouterProvider"
      contains: "RouterProvider"
    - path: "src/routes/Placeholder.tsx"
      provides: "Tailwind v4 token sanity check render (FOUND-02)"
    - path: "src/routes/DesignSystem.tsx"
      provides: "Five-section single-page scroll per D-27"
      min_lines: 100
    - path: "src/hooks/useTheme.ts"
      provides: "Effect hook syncing settings.theme with html data-theme"
      contains: "data-theme"
    - path: "src/hooks/useLang.ts"
      provides: "Hook exposing lang and setLang from the Zustand store"
    - path: "src/components/ThemeToggle.tsx"
      provides: "auto/light/dark Button group using shadcn Button"
      contains: "auto"
    - path: "src/components/LangToggle.tsx"
      provides: "EN/DE select"
    - path: "src/__tests__/App.smoke.test.tsx"
      provides: "FOUND-06 acceptance: App renders without crashing"
      contains: "@vitest-environment jsdom"
    - path: "src/__tests__/DesignSystem.test.tsx"
      provides: "DESIGN-05+DESIGN-06 acceptance: 5 sections render + theme toggle reacts"
      contains: "@vitest-environment jsdom"
  key_links:
    - from: "src/App.tsx"
      to: "src/router.tsx"
      via: "RouterProvider with router prop"
      pattern: "RouterProvider"
    - from: "src/App.tsx"
      to: "src/hooks/useTheme.ts"
      via: "useTheme() call inside App body"
      pattern: "useTheme"
    - from: "src/hooks/useTheme.ts"
      to: "src/lib/storage/store.ts"
      via: "useStore((s) => s.settings.theme) selector"
      pattern: "useStore"
    - from: "src/components/ThemeToggle.tsx"
      to: "src/components/ui/button.tsx + useStore.setTheme"
      via: "import Button + useStore((s) => s.setTheme)"
      pattern: "setTheme"
    - from: "src/routes/DesignSystem.tsx"
      to: "src/styles/theme.css + animations.css"
      via: "Tailwind utility classes consume @theme tokens; inline style triggers each keyframe"
      pattern: "data-keyframe"
---

<objective>
Wire the Phase 1 skeleton together: hash router, theme + language hooks, two toggle components, the placeholder home route, the 5-section DesignSystem reference route (DESIGN-06), and the App smoke test (FOUND-06). This is the first plan where the user can open the new app in a browser and see something react: theme toggle flips colours; language toggle switches t() output; reduced-motion preview toggle freezes animations.

Per D-02 and D-03, Phase 1 wires only `/` and `/design-system`. The full v1.0 route table (SHELL-01) is a Phase 2 concern.

Per D-27, the DesignSystem page has 5 sections: header with theme + lang toggles; palette grid; typography scale; animation gallery with the in-page reduced-motion preview toggle from CONTEXT.md `<specifics>`; surface samples. Per D-28, the only shadcn primitive used is Button (added in plan 02).

Per D-19, useTheme() applies `data-theme` on `<html>` and reacts to `prefers-color-scheme` changes when theme is `auto`. Per D-14, language detection ran once at module load in i18n.ts; the setLang store action keeps the store and the module-level `_lang` in sync.

Per D-09, component-level transitions in this plan (Button hover/focus from shadcn Button + the prm-preview-toggle press feedback) use Tailwind utilities (the shadcn Button comes with `motion-safe:` / `motion-reduce:` aware variants by default). Per D-11, the DesignSystem page renders every animation inline so the reduced-motion behaviour can be eyeballed by toggling the DevTools 'Emulate CSS prefers-reduced-motion: reduce' flag — plan 09 task 9.2 Check 3 walks this exact procedure.

Output: 8 source files + 2 jsdom tests. App smoke passes. DesignSystem theme-toggle test passes. The user can `pnpm run dev`, open http://localhost:5173/#/design-system, and verify visual parity against http://localhost:5173/legacy/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-skeleton/01-CONTEXT.md
@.planning/phases/01-skeleton/01-RESEARCH.md
@.planning/phases/01-skeleton/01-PATTERNS.md
@src/lib/storage/store.ts
@src/lib/storage/types.ts
@src/lib/i18n/i18n.ts
@src/lib/data/data.ts
@src/components/ui/button.tsx
@src/styles/theme.css
@src/styles/animations.css

The Zustand store from src/lib/storage/store.ts exposes settings.theme, settings.lang, setTheme(theme), setLang(lang). The shadcn Button at src/components/ui/button.tsx accepts variant, size, className, and standard button props. Tailwind utility classes consume the @theme tokens emitted by plan 07 (bg-bg, text-primary, font-heading, etc).
</context>

<tasks>

<task type="auto">
  <name>Task 8.1: Write hooks (useTheme, useLang), toggle components (ThemeToggle, LangToggle), and the router with two routes</name>
  <files>src/hooks/useTheme.ts, src/hooks/useLang.ts, src/components/ThemeToggle.tsx, src/components/LangToggle.tsx, src/router.tsx</files>
  <read_first>
    - src/lib/storage/store.ts (useStore actions: setTheme, setLang; selectors: settings.theme, settings.lang)
    - src/lib/storage/types.ts (Settings.theme type, Settings.lang type)
    - src/lib/i18n/i18n.ts (availableLangs, t — already synced by storage.setLang)
    - src/components/ui/button.tsx (shadcn Button signature)
    - .planning/phases/01-skeleton/01-PATTERNS.md (sections covering src/hooks/useTheme.ts, useLang.ts, ThemeToggle.tsx, LangToggle.tsx, router.tsx)
    - .planning/phases/01-skeleton/01-CONTEXT.md (D-02, D-03, D-14, D-19, D-28)
  </read_first>
  <action>
    Create `src/hooks/useTheme.ts` with this exact content. The hook reads settings.theme from the Zustand store, applies data-theme on documentElement, and re-applies on prefers-color-scheme changes when theme is auto.

    ```
    // src/hooks/useTheme.ts
    // D-19: read settings.theme from Zustand store, apply data-theme to <html>, react to prefers-color-scheme in 'auto' mode.
    // Replaces v1.0 applyTheme() (public/legacy/js/app.js:47-57).
    import { useEffect } from 'react'
    import { useStore } from '@/lib/storage/store'

    export function useTheme(): void {
      const theme = useStore((s) => s.settings.theme)

      useEffect(() => {
        if (typeof document === 'undefined') return
        document.documentElement.setAttribute('data-theme', theme)
      }, [theme])

      useEffect(() => {
        if (theme !== 'auto') return
        if (typeof window === 'undefined' || typeof window.matchMedia !== 'function') return
        const mq = window.matchMedia('(prefers-color-scheme: dark)')
        const onChange = (): void => {
          document.documentElement.removeAttribute('data-theme')
          requestAnimationFrame(() => {
            document.documentElement.setAttribute('data-theme', 'auto')
          })
        }
        if (typeof mq.addEventListener === 'function') {
          mq.addEventListener('change', onChange)
          return () => mq.removeEventListener('change', onChange)
        }
        return undefined
      }, [theme])
    }
    ```

    Create `src/hooks/useLang.ts`. The hook exposes { lang, setLang } from the Zustand store; the store's setLang action also calls i18n.setLang to sync the module-level `_lang`.

    ```
    // src/hooks/useLang.ts
    // D-14: read settings.lang from Zustand; expose a setLang callback delegating to the store action.
    import { useStore } from '@/lib/storage/store'
    import type { Settings } from '@/lib/storage/types'

    type Lang = NonNullable<Settings['lang']>

    interface UseLangReturn {
      lang: Lang
      setLang: (next: Lang) => void
    }

    export function useLang(): UseLangReturn {
      const lang = useStore((s) => s.settings.lang ?? 'en')
      const setLang = useStore((s) => s.setLang)
      return { lang, setLang }
    }
    ```

    Create `src/components/ThemeToggle.tsx`. Three-way Button group using the shadcn Button from plan 02 (D-28).

    ```
    // src/components/ThemeToggle.tsx
    // D-28: uses the shadcn Button primitive (from plan 02). Three-way toggle: auto / light / dark.
    import { Button } from '@/components/ui/button'
    import { useStore } from '@/lib/storage/store'
    import type { Settings } from '@/lib/storage/types'

    const OPTIONS: ReadonlyArray<Settings['theme']> = ['auto', 'light', 'dark']

    export function ThemeToggle() {
      const current = useStore((s) => s.settings.theme)
      const setTheme = useStore((s) => s.setTheme)
      return (
        <div role="group" aria-label="Theme" className="inline-flex gap-2">
          {OPTIONS.map((opt) => (
            <Button
              key={opt}
              variant={current === opt ? 'default' : 'outline'}
              size="sm"
              aria-pressed={current === opt}
              onClick={() => setTheme(opt)}
              data-testid={`theme-toggle-${opt}`}
            >
              {opt}
            </Button>
          ))}
        </div>
      )
    }
    ```

    Notes:
    - If the shadcn Button variant `outline` is missing in the new-york style generated by plan 02, swap to `ghost`. The Button file at `src/components/ui/button.tsx` lists the available variants.
    - If TypeScript complains about a JSX return type, omit the explicit `: JSX.Element` annotation and let the compiler infer; React 19 with new types generally infers a return correctly without an annotation.

    Create `src/components/LangToggle.tsx`. Plain select per D-28 (only Button is shadcn in Phase 1).

    ```
    // src/components/LangToggle.tsx
    // D-14, D-28: plain <select> in Phase 1.
    import { useStore } from '@/lib/storage/store'
    import type { Settings } from '@/lib/storage/types'
    import { availableLangs } from '@/lib/i18n/i18n'

    type Lang = NonNullable<Settings['lang']>

    export function LangToggle() {
      const current = useStore((s) => s.settings.lang ?? 'en')
      const setLang = useStore((s) => s.setLang)
      return (
        <label className="inline-flex items-center gap-2 text-sm">
          <span>Language</span>
          <select
            value={current}
            onChange={(e) => setLang(e.target.value as Lang)}
            className="rounded border border-line bg-surface px-2 py-1 text-text"
            data-testid="lang-toggle"
          >
            {availableLangs().map((l) => (
              <option key={l} value={l}>
                {l.toUpperCase()}
              </option>
            ))}
          </select>
        </label>
      )
    }
    ```

    Create `src/router.tsx` with the hash router (D-01, D-02, D-03):

    ```
    // src/router.tsx
    // D-01: React Router v7. D-02: createHashRouter. D-03: only / and /design-system in Phase 1.
    import { createHashRouter } from 'react-router-dom'
    import { Placeholder } from './routes/Placeholder'
    import { DesignSystem } from './routes/DesignSystem'

    export const router = createHashRouter([
      { path: '/', element: <Placeholder /> },
      { path: '/design-system', element: <DesignSystem /> },
    ])
    ```

    Critical do-NOTs:
    - Do NOT use createBrowserRouter — D-02 mandates createHashRouter
    - Do NOT add additional routes — D-03 limits Phase 1
    - Do NOT touch localStorage from useTheme or useLang — that flow goes through the Zustand store and its custom persistence middleware (D-06)

    Run `pnpm run typecheck` and `pnpm run lint`. Both must exit 0.
  </action>
  <verify>
    <automated>test -f src/hooks/useTheme.ts && test -f src/hooks/useLang.ts && test -f src/components/ThemeToggle.tsx && test -f src/components/LangToggle.tsx && test -f src/router.tsx && grep -c "data-theme" src/hooks/useTheme.ts && grep -c "prefers-color-scheme" src/hooks/useTheme.ts && grep -c "matchMedia" src/hooks/useTheme.ts && grep -c "useStore" src/hooks/useLang.ts && grep -c "createHashRouter" src/router.tsx && grep -c "createBrowserRouter" src/router.tsx && grep -c "/design-system" src/router.tsx && grep -c "Placeholder" src/router.tsx && grep -c "DesignSystem" src/router.tsx && grep -c "setTheme" src/components/ThemeToggle.tsx && grep -c "aria-pressed" src/components/ThemeToggle.tsx && grep -c "availableLangs" src/components/LangToggle.tsx && pnpm run typecheck && pnpm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/hooks/useTheme.ts` returns 0
    - `test -f src/hooks/useLang.ts` returns 0
    - `test -f src/components/ThemeToggle.tsx` returns 0
    - `test -f src/components/LangToggle.tsx` returns 0
    - `test -f src/router.tsx` returns 0
    - `grep -c "data-theme" src/hooks/useTheme.ts` returns at least 2 (setAttribute + removeAttribute)
    - `grep -c "prefers-color-scheme" src/hooks/useTheme.ts` returns at least 1
    - `grep -c "matchMedia" src/hooks/useTheme.ts` returns at least 1
    - `grep -c "useStore" src/hooks/useLang.ts` returns at least 1
    - `grep -c "useStore" src/hooks/useTheme.ts` returns 1
    - `grep -c "createHashRouter" src/router.tsx` returns 1
    - `grep -c "createBrowserRouter" src/router.tsx` returns 0
    - `grep -c "/design-system" src/router.tsx` returns at least 1
    - `grep -c "<Placeholder" src/router.tsx` returns 1
    - `grep -c "<DesignSystem" src/router.tsx` returns 1
    - `grep -c "setTheme" src/components/ThemeToggle.tsx` returns at least 1
    - `grep -c "aria-pressed" src/components/ThemeToggle.tsx` returns at least 1
    - `grep -c "availableLangs" src/components/LangToggle.tsx` returns at least 1
    - `pnpm run typecheck` exits 0
    - `pnpm run lint` exits 0
  </acceptance_criteria>
  <done>
    Hooks and toggle components are in place: useTheme keeps html data-theme in sync with the Zustand settings.theme and reacts to prefers-color-scheme flips when theme is auto; useLang exposes lang + setLang; ThemeToggle renders three shadcn Buttons (auto/light/dark) with aria-pressed; LangToggle renders a plain select populated from availableLangs. The hash router wires / and /design-system (D-02, D-03).
  </done>
</task>

<task type="auto">
  <name>Task 8.2: Write the Placeholder and DesignSystem routes (D-27 5-section page with reduced-motion preview toggle) and update App.tsx to use the router + useTheme</name>
  <files>src/App.tsx, src/routes/Placeholder.tsx, src/routes/DesignSystem.tsx</files>
  <read_first>
    - src/lib/data/data.ts (DEFAULT_SCALE)
    - src/lib/i18n/i18n.ts (t — for welcome_title in Placeholder)
    - src/styles/theme.css (token names available to Tailwind utilities — Pitfall 1 — bg-bg requires --color-bg, etc.)
    - src/styles/animations.css (keyframe names and body[data-prm='reduce'] hook)
    - src/hooks/useTheme.ts
    - src/router.tsx
    - src/components/ThemeToggle.tsx
    - src/components/LangToggle.tsx
    - src/components/ui/button.tsx
    - .planning/phases/01-skeleton/01-CONTEXT.md (D-27 five sections + specifics block reduced-motion preview)
  </read_first>
  <action>
    Replace src/App.tsx with the router-mounting version:

    ```
    // src/App.tsx
    // Top-level component: applies the useTheme side effect and mounts the router.
    import { RouterProvider } from 'react-router-dom'
    import { router } from './router'
    import { useTheme } from './hooks/useTheme'

    export default function App() {
      useTheme()
      return <RouterProvider router={router} />
    }
    ```

    Replace src/routes/Placeholder.tsx:

    ```
    // src/routes/Placeholder.tsx
    // FOUND-02 sanity: Tailwind utilities derived from v2.0 @theme tokens resolve here.
    // FOUND-07: this route renders at / while the legacy app is reachable at /legacy/.
    import { Link } from 'react-router-dom'
    import { t } from '@/lib/i18n/i18n'

    export function Placeholder() {
      return (
        <main className="min-h-screen bg-bg text-text font-sans p-8 max-w-3xl mx-auto">
          <h1 className="font-heading text-4xl text-primary">{t('welcome_title')}</h1>
          <p className="mt-4 text-muted">
            Relationshape v2.0 skeleton is alive. Open{' '}
            <Link to="/design-system" className="underline text-accent">
              /design-system
            </Link>{' '}
            for the theme + animation reference.
          </p>
          <p className="mt-2 text-muted text-sm">
            The legacy v1.0 app is still available at{' '}
            <a href="/legacy/" className="underline">
              /legacy/
            </a>{' '}
            for parity checks during the migration.
          </p>
        </main>
      )
    }
    ```

    If `welcome_title` is not the right key (plan 05 may have shipped a different sentinel), use whichever key plan 05's i18n.test.ts exercised.

    Create src/routes/DesignSystem.tsx as the D-27 five-section page:

    ```
    // src/routes/DesignSystem.tsx
    // DESIGN-06: reference route at /design-system. Five sections per D-27:
    //   1. Header with theme + lang toggles
    //   2. Palette grid
    //   3. Typography scale
    //   4. Animation gallery + in-page reduced-motion preview toggle
    //   5. Surface samples
    // CONTEXT.md specifics: the preview toggle adds data-prm='reduce' on body so reviewers can eyeball
    // reduced-motion behaviour without changing OS settings.
    import { useState, useEffect } from 'react'
    import { ThemeToggle } from '@/components/ThemeToggle'
    import { LangToggle } from '@/components/LangToggle'
    import { Button } from '@/components/ui/button'
    import { DEFAULT_SCALE } from '@/lib/data/data'

    const PALETTE_TOKENS: ReadonlyArray<{ name: string; cssVar: string }> = [
      { name: 'bg', cssVar: '--color-bg' },
      { name: 'bg-2', cssVar: '--color-bg-2' },
      { name: 'surface', cssVar: '--color-surface' },
      { name: 'surface-2', cssVar: '--color-surface-2' },
      { name: 'surface-3', cssVar: '--color-surface-3' },
      { name: 'text', cssVar: '--color-text' },
      { name: 'muted', cssVar: '--color-muted' },
      { name: 'primary', cssVar: '--color-primary' },
      { name: 'primary-strong', cssVar: '--color-primary-strong' },
      { name: 'accent', cssVar: '--color-accent' },
      { name: 'green', cssVar: '--color-green' },
      { name: 'red', cssVar: '--color-red' },
      { name: 'glass', cssVar: '--color-glass' },
    ]

    const KEYFRAMES: ReadonlyArray<{ name: string; sample: string }> = [
      { name: 'heroBlobPulse', sample: 'animation: heroBlobPulse 6s ease-in-out infinite' },
      { name: 'holoOrbDrift', sample: 'animation: holoOrbDrift 14s linear infinite' },
      { name: 'holoBtnSpin', sample: 'animation: holoBtnSpin 8s linear infinite' },
      { name: 'holoIconSpin', sample: 'animation: holoIconSpin 9s linear infinite' },
      { name: 'holoUnderlineSlide', sample: 'animation: holoUnderlineSlide 4s linear infinite' },
      { name: 'iridShift', sample: 'animation: iridShift 7s linear infinite' },
      { name: 'bgPulse', sample: 'animation: bgPulse 12s ease-in-out infinite' },
      { name: 'silkShift', sample: 'animation: silkShift 6s linear infinite' },
    ]

    export function DesignSystem() {
      const [prmPreview, setPrmPreview] = useState(false)

      useEffect(() => {
        if (typeof document === 'undefined') return
        if (prmPreview) {
          document.body.setAttribute('data-prm', 'reduce')
        } else {
          document.body.removeAttribute('data-prm')
        }
        return () => {
          document.body.removeAttribute('data-prm')
        }
      }, [prmPreview])

      return (
        <main className="min-h-screen bg-bg text-text font-sans p-8 max-w-5xl mx-auto space-y-16">
          <header className="flex flex-wrap items-center justify-between gap-4" data-section="header">
            <h1 className="font-heading text-4xl text-primary">Design System</h1>
            <div className="flex items-center gap-6">
              <ThemeToggle />
              <LangToggle />
            </div>
          </header>

          <section data-section="palette" aria-labelledby="palette-heading">
            <h2 id="palette-heading" className="font-heading text-2xl text-primary mb-4">1. Palette</h2>
            <div className="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-4">
              {PALETTE_TOKENS.map(({ name, cssVar }) => (
                <div key={name} className="flex flex-col items-center text-sm">
                  <div
                    className="w-24 h-24 rounded-[var(--radius)] border border-line"
                    style={{ background: 'var(' + cssVar + ')' }}
                    data-token={name}
                  />
                  <code className="mt-2 text-muted">{name}</code>
                </div>
              ))}
            </div>
          </section>

          <section data-section="typography" aria-labelledby="typography-heading">
            <h2 id="typography-heading" className="font-heading text-2xl text-primary mb-4">2. Typography</h2>
            <div className="space-y-2">
              <p className="font-heading text-5xl">Playfair Display 48 — display</p>
              <p className="font-heading text-3xl">Playfair Display 32 — heading</p>
              <p className="font-heading text-2xl">Playfair Display 24 — subhead</p>
              <p className="font-sans text-lg">DM Sans 18 — lead</p>
              <p className="font-sans text-base">DM Sans 16 — body</p>
              <p className="font-sans text-sm text-muted">DM Sans 14 — caption</p>
              <p className="font-sans text-xs text-muted">DM Sans 12 — micro</p>
            </div>
          </section>

          <section data-section="animations" aria-labelledby="animations-heading">
            <h2 id="animations-heading" className="font-heading text-2xl text-primary mb-4">3. Animations</h2>
            <div className="mb-4 flex items-center gap-3">
              <Button
                variant={prmPreview ? 'default' : 'outline'}
                size="sm"
                onClick={() => setPrmPreview((v) => !v)}
                data-testid="prm-preview-toggle"
                aria-pressed={prmPreview}
              >
                {prmPreview ? 'Disable reduced-motion preview' : 'Enable reduced-motion preview'}
              </Button>
              <p className="text-sm text-muted">
                Adds <code>data-prm=&quot;reduce&quot;</code> to <code>&lt;body&gt;</code>. All 8 animations should freeze when enabled.
              </p>
            </div>
            <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
              {KEYFRAMES.map(({ name, sample }) => (
                <div key={name} className="rounded-[var(--radius)] border border-line p-4 bg-surface">
                  <code className="block text-sm text-primary">{name}</code>
                  <div
                    className="mt-3 h-16 w-full rounded-md bg-gradient-to-r from-primary via-accent to-primary-strong"
                    style={{ animation: name + ' 6s linear infinite' }}
                    data-keyframe={name}
                  />
                  <p className="mt-2 text-xs text-muted">{sample}</p>
                </div>
              ))}
            </div>
          </section>

          <section data-section="surfaces" aria-labelledby="surfaces-heading">
            <h2 id="surfaces-heading" className="font-heading text-2xl text-primary mb-4">4. Surfaces</h2>
            <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
              <div className="rounded-[var(--radius)] p-6 bg-glass border border-glass-border shadow-[var(--shadow)] backdrop-blur-md">
                <h3 className="font-heading text-xl text-primary">Glass card</h3>
                <p className="text-sm text-muted mt-2">Translucent surface with glass-blur</p>
              </div>
              <div className="rounded-[var(--radius)] p-6 bg-surface shadow-[var(--shadow-glow)] border border-line">
                <h3 className="font-heading text-xl text-primary">Glow card</h3>
                <p className="text-sm text-muted mt-2">Neon glow elevation</p>
              </div>
              <div className="rounded-[var(--radius)] p-6 bg-surface-2 border border-line space-y-2">
                <h3 className="font-heading text-xl text-primary">Button variants</h3>
                <div className="flex gap-2 flex-wrap">
                  <Button size="sm">Default</Button>
                  <Button size="sm" variant="outline">Outline</Button>
                  <Button size="sm" variant="ghost">Ghost</Button>
                </div>
              </div>
            </div>
          </section>

          <section data-section="scale-preview" aria-labelledby="scale-heading">
            <h2 id="scale-heading" className="font-heading text-2xl text-primary mb-4">5. Default scale (data.ts wiring)</h2>
            <div className="flex gap-2 flex-wrap">
              {DEFAULT_SCALE.map((step) => (
                <div
                  key={step.key}
                  className="rounded-full border border-line px-3 py-1 text-xs"
                  style={{ background: step.color, color: '#fff' }}
                >
                  {step.short}
                </div>
              ))}
            </div>
          </section>
        </main>
      )
    }
    ```

    Notes:
    - The five sections required by D-27 are: header (with toggles), palette, typography, animations (with PRM toggle), surfaces. The DEFAULT_SCALE row is a bonus data.ts wiring check.
    - The inline `style={{ animation: name + ' 6s linear infinite' }}` triggers each keyframe live. The reduced-motion preview toggle adds `body[data-prm='reduce']` and animations.css sets `animation: none !important` under that selector.
    - The `bg-bg`, `text-primary`, `bg-glass`, etc. utilities only generate if plan 07 named the @theme tokens with the v4 prefix convention (Pitfall 1).
    - The shadcn Button variants depend on plan 02's task 2.3 output; if `outline` is missing, swap to `ghost`. The Button file at src/components/ui/button.tsx documents the available variants.

    Critical do-NOTs:
    - Do NOT add any shadcn primitive beyond Button — D-28 limits Phase 1
    - Do NOT hardcode hex values that should come from theme.css — use var(--color-*) or Tailwind utilities
    - Do NOT touch localStorage from the route — state writes go through useStore actions

    Run `pnpm run typecheck` and `pnpm run lint` and `pnpm run build`. All three must exit 0.
  </action>
  <verify>
    <automated>test -f src/App.tsx && test -f src/routes/Placeholder.tsx && test -f src/routes/DesignSystem.tsx && grep -c "RouterProvider" src/App.tsx && grep -c "useTheme" src/App.tsx && grep -c "router" src/App.tsx && grep -c "bg-bg" src/routes/Placeholder.tsx && grep -c "ThemeToggle" src/routes/DesignSystem.tsx && grep -c "LangToggle" src/routes/DesignSystem.tsx && grep -c 'data-section="palette"' src/routes/DesignSystem.tsx && grep -c 'data-section="typography"' src/routes/DesignSystem.tsx && grep -c 'data-section="animations"' src/routes/DesignSystem.tsx && grep -c 'data-section="surfaces"' src/routes/DesignSystem.tsx && grep -c "data-prm" src/routes/DesignSystem.tsx && grep -c "heroBlobPulse" src/routes/DesignSystem.tsx && grep -c "silkShift" src/routes/DesignSystem.tsx && pnpm run typecheck && pnpm run lint && pnpm run build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/App.tsx` returns 0
    - `test -f src/routes/Placeholder.tsx` returns 0
    - `test -f src/routes/DesignSystem.tsx` returns 0
    - `grep -c "RouterProvider" src/App.tsx` returns at least 1
    - `grep -c "useTheme" src/App.tsx` returns at least 1
    - `grep -c "router" src/App.tsx` returns at least 1
    - `grep -c "bg-bg" src/routes/Placeholder.tsx` returns at least 1 (Tailwind utility from --color-bg)
    - `grep -c "ThemeToggle" src/routes/DesignSystem.tsx` returns at least 1
    - `grep -c "LangToggle" src/routes/DesignSystem.tsx` returns at least 1
    - `grep -c 'data-section="palette"' src/routes/DesignSystem.tsx` returns 1
    - `grep -c 'data-section="typography"' src/routes/DesignSystem.tsx` returns 1
    - `grep -c 'data-section="animations"' src/routes/DesignSystem.tsx` returns 1
    - `grep -c 'data-section="surfaces"' src/routes/DesignSystem.tsx` returns 1
    - `grep -c "data-prm" src/routes/DesignSystem.tsx` returns at least 1 (CONTEXT.md specifics preview toggle)
    - `grep -c "heroBlobPulse" src/routes/DesignSystem.tsx` returns 1 (animation gallery)
    - `grep -c "silkShift" src/routes/DesignSystem.tsx` returns 1 (all 8 keyframes rendered)
    - `grep -c "DEFAULT_SCALE" src/routes/DesignSystem.tsx` returns at least 1
    - `pnpm run typecheck` exits 0
    - `pnpm run lint` exits 0
    - `pnpm run build` exits 0
    - `wc -l src/routes/DesignSystem.tsx` returns at least 100
  </acceptance_criteria>
  <done>
    App.tsx mounts the router and calls useTheme. Placeholder.tsx renders at / with at least one Tailwind utility derived from the v2.0 @theme tokens. DesignSystem.tsx renders the D-27 five sections (header, palette, typography, animations, surfaces) plus the reduced-motion preview toggle from CONTEXT.md specifics. The build succeeds. All token utilities and animation references resolve.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 8.3: Write the App smoke test (FOUND-06, jsdom env) and the DesignSystem theme-toggle reactivity test (DESIGN-05, DESIGN-06, jsdom env)</name>
  <files>src/__tests__/App.smoke.test.tsx, src/__tests__/DesignSystem.test.tsx</files>
  <read_first>
    - src/App.tsx
    - src/routes/Placeholder.tsx
    - src/routes/DesignSystem.tsx
    - src/components/ThemeToggle.tsx
    - src/lib/storage/store.ts
    - .planning/phases/01-skeleton/01-RESEARCH.md (section "Verified Vitest config (no environmentMatchGlobs)" — the per-file @vitest-environment jsdom directive)
    - .planning/phases/01-skeleton/01-VALIDATION.md (Manual-Only Verifications + Wave 0 Requirements)
  </read_first>
  <behavior>
    - App smoke test: render App; container is truthy; no thrown error during mount.
    - DesignSystem test 1: render DesignSystem; the 5 section elements (with data-section attributes) are all in the DOM.
    - DesignSystem test 2: click theme-toggle-dark; assert document.documentElement.dataset.theme === 'dark'.
    - DesignSystem test 3: click theme-toggle-light; assert document.documentElement.dataset.theme === 'light'.
    - DesignSystem test 4: click prm-preview-toggle; assert document.body.getAttribute('data-prm') === 'reduce'.
    - DesignSystem test 5: click prm-preview-toggle again; assert document.body.getAttribute('data-prm') === null (toggle off).
  </behavior>
  <action>
    Create src/__tests__/App.smoke.test.tsx with the jsdom environment directive on line 1. The smoke test uses MemoryRouter from react-router-dom to bypass the hash router (otherwise jsdom needs to handle hashchange events). Actually for FOUND-06 the simplest correct check is to render App with the real router; createHashRouter works in jsdom because window.location.hash exists.

    ```
    // @vitest-environment jsdom
    // src/__tests__/App.smoke.test.tsx
    // FOUND-06: <App /> renders without crashing in jsdom.
    import { render } from '@testing-library/react'
    import { describe, it, expect } from 'vitest'
    import App from '@/App'

    describe('<App /> smoke (FOUND-06)', () => {
      it('renders without crashing', () => {
        const { container } = render(<App />)
        expect(container).toBeTruthy()
      })
    })
    ```

    Create src/__tests__/DesignSystem.test.tsx. This test renders DesignSystem directly (not through the router) so the test stays focused on the page behaviour. Theme + lang state lives in the Zustand store, which is module-level — we need to reset it between tests via the same pattern plan 06 used.

    ```
    // @vitest-environment jsdom
    // src/__tests__/DesignSystem.test.tsx
    // DESIGN-05: theme toggle applies data-theme to <html> reactively.
    // DESIGN-06: 5 sections render.
    import { render, screen, fireEvent } from '@testing-library/react'
    import { describe, it, expect, beforeEach, vi } from 'vitest'

    class MemoryLocalStorage {
      private store = new Map<string, string>()
      getItem(k: string): string | null {
        return this.store.has(k) ? (this.store.get(k) as string) : null
      }
      setItem(k: string, v: string): void {
        this.store.set(k, String(v))
      }
      removeItem(k: string): void {
        this.store.delete(k)
      }
      clear(): void {
        this.store.clear()
      }
      get length(): number {
        return this.store.size
      }
      key(i: number): string | null {
        return Array.from(this.store.keys())[i] ?? null
      }
    }

    async function renderFresh() {
      vi.resetModules()
      vi.stubGlobal('localStorage', new MemoryLocalStorage())
      const mod = await import('@/routes/DesignSystem')
      return render(<mod.DesignSystem />)
    }

    describe('<DesignSystem /> route (DESIGN-05, DESIGN-06)', () => {
      beforeEach(() => {
        document.documentElement.removeAttribute('data-theme')
        document.body.removeAttribute('data-prm')
        vi.restoreAllMocks()
      })

      it('renders the 5 D-27 sections (header, palette, typography, animations, surfaces)', async () => {
        await renderFresh()
        expect(document.querySelector('[data-section="header"]')).not.toBeNull()
        expect(document.querySelector('[data-section="palette"]')).not.toBeNull()
        expect(document.querySelector('[data-section="typography"]')).not.toBeNull()
        expect(document.querySelector('[data-section="animations"]')).not.toBeNull()
        expect(document.querySelector('[data-section="surfaces"]')).not.toBeNull()
      })

      it('renders all 8 keyframe samples in the animation gallery (DESIGN-03 wiring)', async () => {
        await renderFresh()
        const expected = [
          'heroBlobPulse', 'holoOrbDrift', 'holoBtnSpin', 'holoIconSpin',
          'holoUnderlineSlide', 'iridShift', 'bgPulse', 'silkShift',
        ]
        for (const name of expected) {
          expect(document.querySelector('[data-keyframe="' + name + '"]')).not.toBeNull()
        }
      })

      it('theme toggle reactively updates html data-theme (DESIGN-05)', async () => {
        // Render App so useTheme() in App.tsx runs the effect; but for unit test purposes we mount
        // DesignSystem + manually apply the same effect via useTheme. The cleanest is to mount App.
        vi.resetModules()
        vi.stubGlobal('localStorage', new MemoryLocalStorage())
        const appMod = await import('@/App')
        const App = appMod.default
        render(<App />)

        const darkBtn = await screen.findByTestId('theme-toggle-dark')
        fireEvent.click(darkBtn)
        // useTheme runs an effect; React Testing Library flushes effects synchronously here
        expect(document.documentElement.dataset.theme).toBe('dark')

        const lightBtn = await screen.findByTestId('theme-toggle-light')
        fireEvent.click(lightBtn)
        expect(document.documentElement.dataset.theme).toBe('light')

        const autoBtn = await screen.findByTestId('theme-toggle-auto')
        fireEvent.click(autoBtn)
        expect(document.documentElement.dataset.theme).toBe('auto')
      })

      it('reduced-motion preview toggle adds data-prm="reduce" to body and removes it on second click', async () => {
        await renderFresh()
        const toggle = screen.getByTestId('prm-preview-toggle')
        fireEvent.click(toggle)
        expect(document.body.getAttribute('data-prm')).toBe('reduce')
        fireEvent.click(toggle)
        expect(document.body.getAttribute('data-prm')).toBeNull()
      })
    })
    ```

    Notes:
    - The `// @vitest-environment jsdom` directive MUST be on line 1 of the test file (or in the first block of comments) per Vitest 4 (Pitfall 4).
    - `vi.resetModules()` + dynamic `import()` is needed because the Zustand store is module-level; each test wants a fresh store state.
    - The theme-toggle test renders the full App (not just DesignSystem) because the useTheme effect lives in App.tsx. The DesignSystem subtree is part of the App tree via the router.
    - `screen.findByTestId('theme-toggle-dark')` — needs the router to land on /design-system. createHashRouter resolves window.location.hash; in jsdom the default is empty string which maps to '/'. To navigate to /design-system, set `window.location.hash = '#/design-system'` BEFORE rendering App. Update the test accordingly:

    ```
    it('theme toggle reactively updates html data-theme (DESIGN-05)', async () => {
      window.location.hash = '#/design-system'
      vi.resetModules()
      vi.stubGlobal('localStorage', new MemoryLocalStorage())
      const appMod = await import('@/App')
      const App = appMod.default
      render(<App />)
      const darkBtn = await screen.findByTestId('theme-toggle-dark')
      fireEvent.click(darkBtn)
      expect(document.documentElement.dataset.theme).toBe('dark')
      // ... rest of test
    })
    ```

    If `screen.findByTestId('theme-toggle-dark')` times out (because the router rendered Placeholder, not DesignSystem), the issue is that the hash navigation didn't propagate in time. Two workarounds:
    1. Render `<DesignSystem />` directly (without router) and ALSO render a tiny consumer of useTheme to make the effect run. Or:
    2. Use the MemoryRouter test pattern instead of createHashRouter for this single test.

    The cleanest is to render DesignSystem in isolation and separately invoke the useTheme effect by mounting a tiny wrapper:

    ```
    function TestWrapper() {
      // duplicate of App.tsx useTheme behaviour for the test
      useTheme()
      return <DesignSystem />
    }
    ```

    If both approaches feel fragile, simplify the theme-toggle test to only verify the Zustand state change (not the DOM attribute), and add a SEPARATE direct test for useTheme that calls renderHook and asserts the attribute update. That keeps the test isolated.

    Run:

    ```
    pnpm run typecheck
    pnpm run lint
    pnpm run test -- --run src/__tests__/App.smoke.test.tsx src/__tests__/DesignSystem.test.tsx
    ```

    All must exit 0.

    Critical do-NOTs:
    - Do NOT remove the `// @vitest-environment jsdom` directive — Vitest defaults to node, and DOM tests fail with "document is not defined"
    - Do NOT use `environmentMatchGlobs` in vitest.config.ts — removed in v4 (Pitfall 4)
    - Do NOT share the Zustand store between tests — module reset is required for isolation
  </action>
  <verify>
    <automated>test -f src/__tests__/App.smoke.test.tsx && test -f src/__tests__/DesignSystem.test.tsx && head -2 src/__tests__/App.smoke.test.tsx | grep -c "@vitest-environment jsdom" && head -2 src/__tests__/DesignSystem.test.tsx | grep -c "@vitest-environment jsdom" && grep -c "renders without crashing" src/__tests__/App.smoke.test.tsx && grep -c "data-section" src/__tests__/DesignSystem.test.tsx && grep -c "theme-toggle-dark" src/__tests__/DesignSystem.test.tsx && grep -c "data-prm" src/__tests__/DesignSystem.test.tsx && grep -c "heroBlobPulse" src/__tests__/DesignSystem.test.tsx && pnpm run typecheck && pnpm run lint && pnpm run test -- --run src/__tests__/App.smoke.test.tsx src/__tests__/DesignSystem.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/__tests__/App.smoke.test.tsx` returns 0
    - `test -f src/__tests__/DesignSystem.test.tsx` returns 0
    - `head -2 src/__tests__/App.smoke.test.tsx | grep -c "@vitest-environment jsdom"` returns 1 (Pitfall 4 directive present)
    - `head -2 src/__tests__/DesignSystem.test.tsx | grep -c "@vitest-environment jsdom"` returns 1
    - `grep -c "renders without crashing" src/__tests__/App.smoke.test.tsx` returns 1 (FOUND-06 assertion name)
    - `grep -c "data-section" src/__tests__/DesignSystem.test.tsx` returns at least 5
    - `grep -c "theme-toggle-dark\|theme-toggle-light\|theme-toggle-auto" src/__tests__/DesignSystem.test.tsx` returns at least 1
    - `grep -c "data-prm" src/__tests__/DesignSystem.test.tsx` returns at least 1
    - `grep -c "heroBlobPulse\|data-keyframe" src/__tests__/DesignSystem.test.tsx` returns at least 1
    - `pnpm run typecheck` exits 0
    - `pnpm run lint` exits 0
    - `pnpm run test -- --run src/__tests__/App.smoke.test.tsx` exits 0
    - `pnpm run test -- --run src/__tests__/DesignSystem.test.tsx` exits 0
  </acceptance_criteria>
  <done>
    App.smoke.test.tsx renders App in jsdom and confirms the tree builds without throwing (FOUND-06). DesignSystem.test.tsx confirms all 5 D-27 sections render, all 8 keyframe samples appear, the theme toggle reactively updates html.dataset.theme (DESIGN-05), and the reduced-motion preview toggle adds and removes body[data-prm='reduce'] (DESIGN-04 in-page preview).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| createHashRouter ↔ window.location.hash | Browser-native; jsdom supports it; legacy v1.0 deep links resolve verbatim |
| useStore state ↔ useTheme effect ↔ html data-theme | One-way data flow: state change triggers effect which mutates DOM |
| ThemeToggle Button click ↔ setTheme action ↔ persist middleware | Action dispatches, persist writes localStorage, useTheme effect re-runs |
| User-supplied lang from LangToggle <select> ↔ store + i18n module sync | The store action calls i18n.setLang to keep module-level _lang in sync (D-14) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Spoofing | A v1.0 deep link (e.g. #/profile/abc) lands on the new app and gets a 404 because Phase 1 only wires / and /design-system | accept | D-03 limits Phase 1 explicitly; SHELL-01 in Phase 2 wires the full table. Until then, the user lands at / via createHashRouter's default fallback |
| T-08-02 | Tampering | useTheme races with the initial Zustand hydration — first render flashes with default 'auto' before settings.theme loads from localStorage | accept | Hydration is synchronous in plan 06's persist.ts (read happens at module init); first React render already has the hydrated state |
| T-08-03 | Information Disclosure | DesignSystem.tsx leaks the bare DEFAULT_SCALE colors at runtime | n/a | DEFAULT_SCALE is committed source — not sensitive |
| T-08-04 | Denial of Service | Reduced-motion preview toggle gets stuck on after unmount, freezing animations on subsequent routes | mitigate | The useEffect cleanup removes data-prm on unmount; second click also removes it. Test 4-5 verify the toggle round-trips |
| T-08-05 | Elevation of Privilege | A future plan adds a `<script>` injection point in DesignSystem | accept | Phase 1 page is static content with no dangerouslySetInnerHTML and no user-supplied text rendering. The test renders pass without DOM errors |
</threat_model>

<verification>
After all three tasks complete:

```
# All artifact files present
ls -la src/App.tsx src/router.tsx src/routes/ src/hooks/ src/components/ src/__tests__/

# Full test suite
pnpm run test -- --run

# Manual eyeball check (not gated here — gated by plan 09):
pnpm run dev
# Then open http://localhost:5173/ — Placeholder visible
# Then open http://localhost:5173/#/design-system — 5 sections visible, toggles work
```
</verification>

<success_criteria>
- 10 files exist: useTheme.ts, useLang.ts, ThemeToggle.tsx, LangToggle.tsx, router.tsx, App.tsx, Placeholder.tsx, DesignSystem.tsx, App.smoke.test.tsx, DesignSystem.test.tsx
- useTheme keeps html data-theme in sync with the Zustand settings.theme and reacts to prefers-color-scheme when theme is auto (D-19)
- useLang exposes { lang, setLang } (D-14)
- ThemeToggle is three shadcn Buttons (auto/light/dark) with aria-pressed (D-28)
- LangToggle is a plain select with EN+DE (D-28)
- router.tsx uses createHashRouter with exactly / and /design-system (D-02, D-03)
- App.tsx mounts the router and calls useTheme
- Placeholder renders Tailwind utilities derived from v2.0 @theme tokens (FOUND-02 sanity)
- DesignSystem renders D-27's 5 sections (header, palette, typography, animations, surfaces) plus the reduced-motion preview toggle from CONTEXT.md specifics, and renders all 8 keyframes live
- App.smoke.test.tsx passes (FOUND-06)
- DesignSystem.test.tsx passes (DESIGN-05, DESIGN-06 in-page assertions)
- All tests pass; `pnpm run typecheck` + `pnpm run lint` + `pnpm run build` exit 0
- Requirements FOUND-06, DESIGN-05, DESIGN-06 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-skeleton/01-08-SUMMARY.md` documenting:
- All test assertion names and pass/fail status (1 smoke + at least 4 DesignSystem assertions)
- A console snapshot of `pnpm run dev` showing Vite started successfully
- Confirmation that the manual checks (open / in browser, open /#/design-system, toggle theme/lang/PRM) all work — the user runs these via plan 09
- Pointer to plan 09 (Wave 6: full-suite verification + post-build grep + SW-scope eyeball)
</output>
