---
phase: 01-skeleton
plan: 09
type: execute
wave: 6
depends_on:
  - "01-08"
files_modified:
  - scripts/check-no-google-fonts.sh
  - package.json
autonomous: false
requirements:
  - FOUND-01
  - FOUND-04
  - FOUND-07
  - CORE-07
  - DESIGN-02
  - DESIGN-03
  - DESIGN-04
  - DESIGN-05
  - DESIGN-06
tags:
  - verification
  - build
  - pwa
  - service-worker
  - reduced-motion
  - manual-eyeball

must_haves:
  truths:
    - "pnpm run typecheck && pnpm run lint && pnpm run test -- --run && pnpm run build all exit 0 — the full Phase 1 gate"
    - "scripts/check-no-google-fonts.sh greps dist/ for fonts.googleapis.com OR fonts.gstatic.com and exits non-zero on ANY match (DESIGN-02 phase-final check)"
    - "package.json scripts add `verify` script that chains typecheck + lint + test + build + check-no-google-fonts.sh (Phase 1 single-command gate)"
    - "Manual eyeball verifications all pass: (1) / serves the React placeholder, (2) /legacy/ still serves v1.0 vanilla app (CORE-07 FOUND-07), (3) DevTools shows exactly one SW per scope after dev/build (Pitfall 3), (4) #/design-system renders all 5 sections, (5) theme toggle flips data-theme reactively (DESIGN-05), (6) reduced-motion preview toggle freezes all 8 animations (DESIGN-04), (7) Chrome DevTools 'Emulate CSS prefers-reduced-motion: reduce' produces the same effect (DESIGN-04 real-world), (8) eyeball animations against /legacy/ side-by-side and confirm parity (DESIGN-03)"
    - "All 21 Phase 1 requirement IDs (FOUND-01..07 + CORE-01..08 + DESIGN-01..06) are satisfied per the prior plans' tests + this plan's verification"
  artifacts:
    - path: "scripts/check-no-google-fonts.sh"
      provides: "Post-build grep guard — fails the build if Google Fonts CDN references slip into dist/"
      contains: "fonts.googleapis.com"
    - path: "package.json"
      provides: "Adds the `verify` script that chains the full Phase 1 gate"
      contains: "\"verify\":"
  key_links:
    - from: "scripts/check-no-google-fonts.sh"
      to: "dist/"
      via: "grep -r recursive scan after pnpm run build"
      pattern: "fonts.googleapis.com"
    - from: "package.json scripts.verify"
      to: "typecheck + lint + test + build + check-no-google-fonts.sh"
      via: "&& chain"
      pattern: "&& "
---

<objective>
Run the final Phase 1 verification gate and capture the manual eyeball confirmations that cannot be automated in a headless environment. This plan is one autonomous task (build + script + verify gate) plus one human-action checkpoint (the eyeball checklist).

The 8 success criteria from ROADMAP.md §"Phase 1: Skeleton" are checked here. Plan 02 already gated `npm run typecheck/lint/build`; plans 04-08 added Vitest coverage for crypto, data, i18n, storage, themes, animations, App smoke, DesignSystem reactivity. This plan re-runs the full suite, adds a DESIGN-02 phase-final grep guard, and walks the user through the manual checks documented in 01-VALIDATION.md `## Manual-Only Verifications`.

Output: A 5-line bash script + a one-line `verify` npm script + a documented eyeball pass. After this plan completes, Phase 1 is shippable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-skeleton/01-VALIDATION.md
@.planning/phases/01-skeleton/01-CONTEXT.md
@.planning/phases/01-skeleton/01-RESEARCH.md
@.planning/codebase/CONCERNS.md

The 8 ROADMAP success criteria for Phase 1:
1. `pnpm run dev` serves React at / and legacy at /legacy/; `pnpm run build`/`test`/`typecheck`/`lint` all green
2. tailwind.config.ts present; sample utility applies; shadcn initialised; Button addable; vite-plugin-pwa configured
3. v1.0 bundle round-trips byte-for-byte via Vitest (CORE-04)
4. v1.0 localStorage hydrates to identical in-memory state (CORE-08); QuotaExceededError surfaces typed (CORE-02); migrateScale + custom-items + __hidden parity (CORE-07)
5. t() resolves every EN+DE key (CORE-06); getLocalizedDefaultScale returns DE scale in DE
6. Every :root token from style.css expressed in @theme; light + dark + auto via data-theme; zero Google Fonts CDN refs in dist (DESIGN-01, DESIGN-02)
7. All 8 keyframes reproduced; prefers-reduced-motion guard disables them (DESIGN-03, DESIGN-04)
8. /design-system route renders palette + typography + animation scale; theme toggle reactive across the tree (DESIGN-05, DESIGN-06)
</context>

<tasks>

<task type="auto">
  <name>Task 9.1: Write the DESIGN-02 post-build grep guard script + add the `verify` npm script + run the full Phase 1 gate (typecheck + lint + test + build + grep)</name>
  <files>scripts/check-no-google-fonts.sh, package.json</files>
  <read_first>
    - package.json (current scripts — append `verify`, do not replace)
    - .planning/phases/01-skeleton/01-VALIDATION.md (`## Sampling Rate` — full suite command + ~90s feedback latency)
    - .planning/codebase/CONCERNS.md (Security §"Google Fonts external dependency" — privacy claim hinge)
    - vite.config.ts (confirms vite-plugin-pwa is configured — sanity)
  </read_first>
  <action>
    Create the directory `scripts/` if not present.

    Write `scripts/check-no-google-fonts.sh`:

    ```
    #!/usr/bin/env bash
    # DESIGN-02 phase-final guard: assert that the production bundle contains zero references
    # to Google Fonts CDN. v1.0's privacy claim was technically inaccurate; v2.0 closes the gap
    # by self-hosting via @fontsource-variable/* packages.
    #
    # Exits 0 if no matches found. Exits non-zero (with diagnostic output) if any reference exists.
    set -euo pipefail

    DIST_DIR="${1:-dist}"

    if [ ! -d "$DIST_DIR" ]; then
      echo "ERROR: dist directory not found at $DIST_DIR — run 'pnpm run build' first" >&2
      exit 2
    fi

    # grep -R follows symlinks if any; -l lists matching filenames; -E for alternation
    # The legacy app under dist/legacy/ DOES legitimately reference Google Fonts (v1.0 HTML);
    # we exclude that path from the check.
    MATCHES=$(grep -RlE "fonts\\.googleapis\\.com|fonts\\.gstatic\\.com" "$DIST_DIR" --exclude-dir=legacy 2>/dev/null || true)

    if [ -n "$MATCHES" ]; then
      echo "FAIL: Google Fonts CDN references found in $DIST_DIR (excluding legacy/):" >&2
      echo "$MATCHES" | sed 's/^/  /' >&2
      echo "" >&2
      echo "DESIGN-02 requires zero references. Fix by:" >&2
      echo "  1. Confirm src/main.tsx imports '@fontsource-variable/dm-sans' and '@fontsource-variable/playfair-display'" >&2
      echo "  2. Confirm no <link href='https://fonts.googleapis.com/...'> in index.html" >&2
      echo "  3. Confirm src/styles/theme.css does not @import or url() a Google Fonts URL" >&2
      exit 1
    fi

    echo "OK: no Google Fonts CDN references in $DIST_DIR (excluding legacy/) — DESIGN-02 holds"
    ```

    Make it executable:

    ```
    chmod +x scripts/check-no-google-fonts.sh
    ```

    Append a `verify` script to package.json. The cleanest is to set it via `npm pkg set`, but the executor can also edit the JSON directly. The script should be:

    ```
    "verify": "pnpm run typecheck && pnpm run lint && pnpm run test -- --run && pnpm run build && bash scripts/check-no-google-fonts.sh"
    ```

    If using `npm pkg set`:

    ```
    npm pkg set scripts.verify="pnpm run typecheck && pnpm run lint && pnpm run test -- --run && pnpm run build && bash scripts/check-no-google-fonts.sh"
    ```

    Now run the full Phase 1 gate:

    ```
    pnpm run verify
    ```

    Capture the output. Every step must exit 0. The grep guard prints "OK: no Google Fonts CDN references" as its final line.

    If `pnpm run verify` fails at ANY step:
    - typecheck/lint failure → fix the offending file (revisit the upstream plan and add the missing type/annotation)
    - test failure → read the assertion message; the upstream plan that authored the test is responsible. Re-run that plan's test in isolation to reproduce
    - build failure → the most common cause is a `.tsx` file using a Tailwind utility class derived from a token that doesn't exist in theme.css (Pitfall 1). Open the failing file, identify the utility (e.g., `bg-bg`), confirm the @theme block has `--color-bg`
    - DESIGN-02 grep guard failure → follow the diagnostic the script prints

    Do NOT skip the build step or run an abbreviated suite. The Phase 1 verifier needs the same gate as ongoing development.

    Critical do-NOTs:
    - Do NOT add `--no-verify` flags anywhere
    - Do NOT skip the `--exclude-dir=legacy` in the grep — `public/legacy/index.html` legitimately references Google Fonts (it's the v1.0 file at the time of capture); DESIGN-02 is about the new app's production bundle, not the legacy passthrough
    - Do NOT modify any source file from another plan to "make verify pass" — if a test fails, fix the production code or document the failure to a follow-up plan
  </action>
  <verify>
    <automated>test -f scripts/check-no-google-fonts.sh && test -x scripts/check-no-google-fonts.sh && grep -c "fonts.googleapis.com" scripts/check-no-google-fonts.sh && grep -c "fonts.gstatic.com" scripts/check-no-google-fonts.sh && grep -c "exclude-dir=legacy" scripts/check-no-google-fonts.sh && grep -c '"verify":' package.json && grep -c "pnpm run typecheck && pnpm run lint" package.json && pnpm run verify</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/check-no-google-fonts.sh` returns 0
    - `test -x scripts/check-no-google-fonts.sh` returns 0 (script is executable)
    - `grep -c "fonts.googleapis.com" scripts/check-no-google-fonts.sh` returns at least 1
    - `grep -c "fonts.gstatic.com" scripts/check-no-google-fonts.sh` returns at least 1
    - `grep -c "exclude-dir=legacy" scripts/check-no-google-fonts.sh` returns 1 (legacy passthrough is exempt)
    - `grep -c '"verify":' package.json` returns 1
    - `grep -c "typecheck && pnpm run lint && pnpm run test" package.json` returns 1 (full chain in verify script)
    - `pnpm run verify` exits 0 — this is the whole-Phase-1 automated gate
    - `pnpm run verify` final line includes "OK: no Google Fonts CDN references" (DESIGN-02 phase-final pass)
    - After the build, `test -f dist/sw.js` returns 0 (FOUND-04 still passes)
    - After the build, `test -f dist/manifest.webmanifest` returns 0
    - After the build, `test -f dist/legacy/index.html` returns 0 (legacy passthrough still copied into dist; CORE-07)
    - After the build, `grep -c '"/legacy/' dist/sw.js` returns 0 (vite-plugin-pwa workbox.globIgnores: ['legacy/**'] is honoured — Pitfall 2)
  </acceptance_criteria>
  <done>
    `scripts/check-no-google-fonts.sh` exists, is executable, excludes the legacy passthrough, and exits non-zero on any DESIGN-02 violation. The `verify` script in package.json chains typecheck + lint + test + build + grep. `pnpm run verify` exits 0, proving the entire Phase 1 automated surface is green: TypeScript strict mode passes, ESLint is clean, all Vitest suites (crypto, data, i18n, storage, migrateScale, theme-tokens, animations, App smoke, DesignSystem) pass, the build produces dist/sw.js + dist/manifest.webmanifest, and the dist/ tree (excluding legacy/) contains zero Google Fonts references.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 9.2: HUMAN VERIFICATION — Manual eyeball checks per 01-VALIDATION.md `## Manual-Only Verifications`</name>
  <what-built>
    Plans 01 through 08 plus task 9.1 have shipped the Phase 1 skeleton: legacy app under `/legacy/`, new app at `/`, hash router with `/design-system`, theme + lang toggles, 5-section DesignSystem reference page with reduced-motion preview, all 8 keyframes live, 47+ Vitest assertions green. Task 9.1 ran the automated gate (`pnpm run verify`) and it passed.

    This task is the manual eyeball pass for the 4 verifications that cannot be automated in a headless environment per VALIDATION.md.
  </what-built>
  <how-to-verify>
    Open a terminal in the repo root and run:

    ```
    pnpm run dev
    ```

    Vite starts on http://localhost:5173/. Then walk through these checks in a real browser (Chrome or Safari):

    **Check 1 — FOUND-01 + FOUND-07: dual-app serving + SW scope (Pitfall 3)**

    1. Open http://localhost:5173/ in a browser. You should see the React Placeholder: "Relationshapes" heading (via t('welcome_title')), a link to /design-system, and a mention of the /legacy/ URL.
    2. Open http://localhost:5173/legacy/ in a new tab. You should see the v1.0 Relationshape vanilla-JS app — same content as before the migration started.
    3. DevTools → Application → Service Workers. Confirm:
       - If you ran `pnpm run dev`, vite-plugin-pwa has `devOptions.enabled: false` (D-23), so the new app's SW will NOT register in dev. This is correct.
       - The legacy SW at /legacy/sw.js MAY register if you previously visited /legacy/. Confirm its scope shows `/legacy/`, not `/`. If it shows `/`, that is a stale registration from before plan 01-01's move — clear it (Application → Service Workers → Unregister) and reload /legacy/.
    4. Run `pnpm run build && pnpm run preview` to test the production SW too. Open http://localhost:5173/ (or whatever port `preview` chose). DevTools → Application → Service Workers should now show one SW at scope `/` (the new vite-plugin-pwa one) and a separate one at `/legacy/` if you also visit /legacy/.

    **Check 2 — DESIGN-03: 8 keyframes visually match v1.0**

    1. Open http://localhost:5173/#/design-system. Scroll to section 3 (Animations).
    2. Open http://localhost:5173/legacy/ in an adjacent tab. The hero blob and any animated affordances on that page show the v1.0 reference behaviour.
    3. For each of the 8 keyframes shown in the DesignSystem gallery (heroBlobPulse, holoOrbDrift, holoBtnSpin, holoIconSpin, holoUnderlineSlide, iridShift, bgPulse, silkShift), eyeball that the timing + motion shape match what /legacy/ shows. They don't need to be pixel-perfect (different surfaces) but the cadence and direction should match.

    **Check 3 — DESIGN-04: prefers-reduced-motion disables all 8 keyframes**

    1. On the /design-system page, click the "Enable reduced-motion preview" Button (top of section 3). Confirm all 8 keyframe samples freeze — no motion at all. This tests the in-page `body[data-prm='reduce']` selector.
    2. Then test the real `prefers-reduced-motion` media query: Chrome DevTools → Rendering → Emulate CSS media feature → set `prefers-reduced-motion: reduce`. Reload the page. Confirm all 8 keyframes freeze again — this time via the @media block in animations.css. WCAG 2.1 SC 2.3.3 compliance check.

    **Check 4 — DESIGN-05: theme toggle is reactive across the tree**

    1. On the /design-system page, click the "auto" / "light" / "dark" Buttons in the header (section 1) in sequence.
    2. Each click should:
       - Visibly flip the entire page's background, text, and accent colors (the @theme tokens swap via the `[data-theme]` selector)
       - Update DevTools → Elements: `<html data-theme="light">` (or dark / auto) on every click
       - Persist: open DevTools → Application → Local Storage → http://localhost:5173 → key `relationshape.v1` → confirm settings.theme matches the last clicked value
    3. Reload the page. The chosen theme persists from localStorage.
    4. Set Mode to "auto" and Chrome DevTools → Rendering → Emulate CSS prefers-color-scheme → toggle between dark and light. Confirm the page re-renders with the OS preference (the useTheme hook's mq listener fires; D-19).

    **Check 5 — DESIGN-06: /design-system route renders all sections + language toggle reacts**

    1. Already covered by the section-walk in Check 3. Additionally: click the language `<select>` next to ThemeToggle in the header and switch to "DE". Confirm that any `t()`-driven string in DesignSystem updates. In Phase 1, the only `t()` call in routed content is `welcome_title` on Placeholder (since DesignSystem doesn't use t() for its labels — the labels are English-only structural copy). Navigate back to `/` after switching: the heading should now show the DE translation of welcome_title (e.g. "Beziehungsformen" or whatever plan 05's port translated).

    **Check 6 — CORE-07: legacy coexistence still works**

    1. Navigate to http://localhost:5173/legacy/, create a profile, answer a few questions, refresh. Data persists.
    2. Navigate to http://localhost:5173/ — the new app does NOT yet show that profile (Phase 2 wires the views; Phase 1 only ports the underlying types). But DevTools → Application → Local Storage shows `relationshape.v1` contains the profile data you entered via /legacy/. This proves the localStorage key is shared correctly (D-22).

    After completing all 6 checks, report results:
    - Check 1: PASS / FAIL (note any SW scope anomalies)
    - Check 2: PASS / FAIL (note any animation mismatches)
    - Check 3: PASS / FAIL (in-page toggle AND DevTools emulation)
    - Check 4: PASS / FAIL (3 modes + persistence + auto reactivity)
    - Check 5: PASS / FAIL (DE switch round-trip)
    - Check 6: PASS / FAIL (localStorage sharing)
  </how-to-verify>
  <resume-signal>
    Type `approved` if all 6 manual checks pass. If any check fails, describe which one + the symptom + your browser/OS, and we will diagnose. Common failure modes:

    - Check 1 SW scope: stale registration from before plan 01-01 — unregister via DevTools and retry
    - Check 2 animation mismatch: most likely the keyframe body in animations.css doesn't match v1.0 — open public/legacy/css/additions.css at the documented line numbers and copy the exact percentages/transform values into src/styles/animations.css
    - Check 3 animations don't freeze: animations.css `animation: none !important` selector list might not target the specific element receiving the keyframe in the DesignSystem gallery — the gallery uses inline style, so check that the universal `*` rule under `body[data-prm='reduce']` matches
    - Check 4 theme doesn't persist: confirm storage.test.ts passed (plan 06) and that useTheme is called inside App.tsx (not just imported)
    - Check 5 DE doesn't switch: confirm i18n.test.ts passed (plan 05) and that storage.setLang dispatches i18n.setLang
    - Check 6 data doesn't appear: this is the intended Phase 1 state — Phase 2 (PROFILE-* requirements) wires the new app to show that data. The check only verifies the SHARED localStorage key works
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `pnpm run build` output (dist/) → static host | Every file under dist/ is what gets deployed; the grep guard is the last automated check |
| Manual eyeball (browser + DevTools) → human reviewer | All visual + reactivity checks documented in VALIDATION.md as Manual-Only |
| Service worker scope (/) ↔ legacy SW scope (/legacy/) | The new app's SW (vite-plugin-pwa) should NOT precache /legacy/* (Pitfall 2); the legacy SW should claim only /legacy/* (Pitfall 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | A late edit reintroduces fonts.googleapis.com — the privacy claim regresses | mitigate | scripts/check-no-google-fonts.sh runs at every `pnpm run verify`; the verify script is the canonical CI gate; CI / pre-commit hooks (Phase 3 concern) will adopt the same script |
| T-09-02 | Denial of Service | The legacy SW (rshape-v9 cache) and the new SW (vite-plugin-pwa generated) compete for / scope, serving stale content | mitigate | Pitfall 3 mitigation (clientsClaim: true) + Pitfall 2 mitigation (globIgnores: legacy/**) — both checked manually here (Check 1) |
| T-09-03 | Spoofing | A v1.0 deep link like #/profile/abc opens in Phase 1 and shows a 404-equivalent (router's default `/` route) | accept | D-03 limits Phase 1 to / and /design-system; the user already knows; Phase 2 fixes this |
| T-09-04 | Tampering | A future plan removes the `verify` script or weakens it | mitigate | `verify` script is committed to package.json; code review catches a removal; the script is also documented in this plan's SUMMARY |
| T-09-05 | Repudiation | A team member runs `npm run verify` locally but does NOT confirm the eyeball checks — Phase 1 ships with a hidden visual regression | accept | Manual checks are explicitly documented in VALIDATION.md and this plan's checkpoint; the developer + the user (solo workflow) jointly own the eyeball pass — no broader team to delegate to |
</threat_model>

<verification>
After both tasks complete:

```
# The automated gate must be green
pnpm run verify

# All 8 ROADMAP success criteria pass (cross-reference)
# 1. Build + test + typecheck + lint green ← verify script
# 2. Tailwind + shadcn + vite-plugin-pwa wired ← test -- --run + dist/sw.js present
# 3. Crypto round-trip ← plan 04 crypto.test.ts (covered in verify)
# 4. Storage hydrate + quota + migrate ← plan 06 storage.test.ts + migrateScale.test.ts
# 5. i18n EN+DE parity ← plan 05 i18n.test.ts
# 6. Theme tokens + zero Google Fonts ← plan 07 theme-tokens.test.ts + check-no-google-fonts.sh
# 7. 8 keyframes + reduced-motion ← plan 07 animations.test.ts + this plan's Check 3
# 8. /design-system route reactive ← plan 08 DesignSystem.test.tsx + this plan's Check 4-5
```
</verification>

<success_criteria>
- scripts/check-no-google-fonts.sh exists, is executable, and exits non-zero on any DESIGN-02 violation (excluding legacy/)
- package.json defines the `verify` script chaining typecheck + lint + test + build + grep
- `pnpm run verify` exits 0 — full Phase 1 automated gate is green
- All 6 manual eyeball checks pass (Check 1-6 in task 9.2)
- All 21 Phase 1 requirement IDs (FOUND-01..07, CORE-01..08, DESIGN-01..06) are satisfied per their respective tests
- All 8 ROADMAP §"Phase 1: Skeleton" success criteria are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-skeleton/01-09-SUMMARY.md` documenting:
- Exact `pnpm run verify` final-line output
- Six PASS / FAIL flags for the manual eyeball checks
- Total assertion count across all Phase 1 test files (sum of plan 04-08 test counts)
- A confirmation that `dist/legacy/` is present in the build output and that `dist/sw.js` does NOT reference `/legacy/`
- A note that the Phase 1 milestone is shippable

After all 9 SUMMARYs exist, the orchestrator runs `/gsd-transition` to mark Phase 1 complete and transition to Phase 2 (Parity).
</output>
