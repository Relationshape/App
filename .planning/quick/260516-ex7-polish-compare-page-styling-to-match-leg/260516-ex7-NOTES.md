---
quick_id: 260516-ex7
status: scoping
---

# Investigation Notes

## Screenshots show
1. **Compare page** (`#/compare?ids=`):
   - Title "📊 Vergleichen" + sub "Wähle bis zu vier Ergebnisse..."
   - Pink chip "🍀 Test Subject → Test Map" (one result selected)
   - Purple tip callout: "Tipp: Aktiviere den „Fabi-Modus" in den Einstellungen..."
   - "Kategorie-Details" h2 with sub "Jede Karte öffnet ein Netzdiagramm..."
   - "Weitere Kategorien hinzufügen" button
   - 3 category cards (Allgemeine Verbindung / Emotionale Intimität / Partner & Begleitung) with icon + title + blurb + → arrow

2. **Category modal** (opens on card click):
   - Header: icon + cat title + blurb
   - Tabs: Netzdiagramm | Element für Element | Antworten bearbeiten
   - Spider chart with axis labels — **text is too large** in current modal
   - Subject chip at bottom
   - Footer: Schließen | Änderungen speichern

## Current React state (`src/routes/Compare.tsx`)
- 119 lines. Has chips + inline Spider + inline drilldown (ItemSpider + CategoryBars).
- **Missing**: cat-grid section, category modal, Fabi-mode tip callout, default-to-first-two-results behavior.
- Uses Tailwind classes that don't match the polished legacy styling.

## Legacy reference (`public/legacy/js/app.js`)
- `viewCompare(ids)` at line 3453 — full structure
- `categoryCards(datasets, editableResult, filterCatIds)` at line 2839 — card rendering
- `openCategoryModal(datasets, cat, editableResult)` at line 2879 — modal with tabs

## Legacy CSS (`src/styles/legacy-components.css`)
- `.cat-grid` `.cat-card` `.cat-card-head` `.cat-card-icon` `.cat-card-titles` `.cat-card-toggle` — all exist
- `.compare-pick` `.pick-chip` — all exist
- `.callout` `.muted` — exist
- `.page-head` `.page-section` `.section-head` — exist

## Modal text-too-large issue
- The user describes clicking a box opens a modal with the spider diagram and the text labels are too large.
- The legacy `openCategoryModal` uses `renderItemSpider(datasets, cat.id, { size: 520 })`.
- React equivalent: `<ItemSpider />` at `src/components/charts/ItemSpider.tsx`. Need to verify its label font-size and ensure when used in a modal context it's appropriately sized.

## Scope decision
This is bigger than a small fix:
- Need to rewrite Compare.tsx to use legacy CSS classes + add cat-grid section.
- Need to create CategoryModal React component (uses shadcn Dialog, tabs).
- Need to fix ItemSpider label sizing for modal contexts.

Single plan, 3 tasks: (1) Compare page redesign, (2) CategoryModal component, (3) ItemSpider sizing fix.
